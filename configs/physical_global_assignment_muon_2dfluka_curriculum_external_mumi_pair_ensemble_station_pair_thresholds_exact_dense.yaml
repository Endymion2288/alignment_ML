# Denser, still finite validation-only operating-point audit.  It checks that
# a five-point threshold grid or one Sinkhorn temperature was not hiding a
# feasible nominal pairwise-MLP point.  The exact Pareto selector keeps the
# search reproducible and test-sealed.
base_config: physical_global_assignment_muon_2dfluka_curriculum_external_mumi_pair_ensemble_station_pair_thresholds_exact.yaml

physical_curriculum_mlp:
  station_pair_threshold_baseline:
    unmatched_penalties: [0.0, 0.5, 1.0]
    sinkhorn_temperatures: [0.2, 0.5, 1.0]
    threshold_grid: [0.0, 0.001, 0.005, 0.01, 0.02, 0.05, 0.10, 0.20, 0.30, 0.35, 0.40, 0.50, 0.60, 0.70, 0.80, 0.90]
    initial_thresholds: [0.10, 0.35]
