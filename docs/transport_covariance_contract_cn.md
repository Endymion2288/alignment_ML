# T11 传播协方差契约

`[x,y,tx,ty]` 与 `q/p`（原生 per MeV）的数学换算在此测试。生产 `C_prop` 仍是
冻结的 WB87 负结果 `faseracts_transport_covariance_not_validated`。

不重跑 Stage B，不提交 18 源作业，不 rescale 协方差，不用 truth q/p 当部署
种子。T12 不得把本契约当成已建立。
