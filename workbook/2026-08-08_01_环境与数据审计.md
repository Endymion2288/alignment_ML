# 2026-08-08：环境与数据审计

## 已完成

1. 节点为 `lxplus905`，GPU 为 Tesla T4（15 GB）。
2. 已确认 ML 环境：
   `/cvmfs/sft.cern.ch/lcg/views/LCG_110_cuda/x86_64-el9-gcc13-opt/setup.sh`。
   Python 3.13.11、ROOT 6.40.02、PyTorch 2.11.0/CUDA 12.5、uproot 5.7.1、
   awkward 2.9.0；GPU tensor smoke test 成功。
3. Calypso 未编译，但 Athena 24.0.41 / `x86_64-el9-gcc13-opt` 可由
   `asetup` 取得。
4. 真实 IFT PHYS 和 MC24 xAOD/PHYS 输入路径已确认，详细字段表见
   `docs/data_audit_cn.md`。
5. Calypso 源码确认 station 的可靠来源是关联 cluster 的
   `FaserSCT_ID::station()`。geometry factory 的 station 顺序为
   `Interface`、`StationA`、`StationB`、`StationC`。
6. 建立了 canonical flat ROOT schema、严格 loader、straight-line
   chi2 gate 与 greedy one-to-one baseline；Transformer 尚未开始。

## 当前结论

现有 PHYS NTuple 不能直接用于科学 chi2 baseline 或 MC association
pretraining：缺少 segment-level station、协方差、hit 摘要和 truth barcode。
不能由 z 值或全局 track truth 分支补猜这些字段。

## 下一步

1. 编译 Calypso。
2. 在 NtupleDumper 中实现专用 tracklet exporter，并对 station、native/global
   covariance 变换和 segment truth matching 做小样本验证。
3. 用导出的 MC canonical ROOT 跑 baseline，记录 efficiency、purity、
   fake rate 和 residual/chi2。

