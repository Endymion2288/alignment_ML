# Workbook 102: Task B6 CKF 重建输入契约审计

日期：2026-09-06
状态：**完成** —— 已建立 raw CKF 与 long-track 等价样本的输入范围对照。未实现过滤器，未修 closure，未删尾巴，未设计 cut，未调 C/Q，未进入 Measurement Model V2 / alignment / ML。未改 WB87–WB101 结论。

**最终判定：`AUDITED`（主因 Case A）**

- `decision = ckf_reconstruction_contract_audited`
- `primary_case = reconstruction_input_contract_missing`
- `secondary_cases = ckf_fitting_quality`
- `next_step = transport_covariance_v3_after_input_scope`
- `closure_pass = false`
- `closure_claimed_after_subset = false`
- `input_filter_implemented = false`
- `measurement_model_v2_entered = false`

冻结保持：

- WB96：`ckf_qoverp_covariance_export_established`
- WB98：`acts_q_materialized_closure_failed`
- WB99：`high_chi2_tail_dominated`
- WB100：`wrong_track_state_dominated`
- WB101：`ckf_tail_provenance_audited` / `ckf_reconstruction_failure`

## 起始状态

HEAD（任务开始时）：`32c9044a8543845aaa0767d8089ba6fd731f31a9`

相对 WB101：只新增本任务文件。未覆盖 WB98 dumps、WB99–WB101 产物。名单是 WB99 official pair top 1%（24）/ 0.1%（3），与 WB101 provenance keys 一致，未重新筛选。

## 验收标准

| 项 | 要求 | 结果 |
| --- | --- | --- |
| B6.1 输入范围 | `ckf_input_scope_audit.json` | **完成**。895 条唯一 CKF，779 条 long track，116 条被拒。 |
| B6.2 契约 | 只定义，不实现过滤 | **完成**。`eligible_for_transport_validation` 未落地。 |
| B6.3 尾巴对照 | 冻结名单；不得写成 PASS | **完成**。long-track 子集 2094 行，剩余冻结 9 行。`closure_pass = false`。 |
| B6.4 短径迹 | 15/24 非 long-track | **完成**。缺站 5、无 truth 匹配 4、other 6。 |
| 分类 | Case A / B / C | **Case A**。次因 B。 |
| 禁止项 | 不进 V2，不调 C/Q，不删尾巴 | 保持。 |

## B6.1 CKFTrackCollection vs longTracks

全部 9 个 WB87 源。身份 `(source_id, run_id, event_id, track_index)`。

| 量 | 计数 |
| --- | --- |
| 唯一 CKF | 895 |
| 对应 ntuple long track | 779 |
| CKF 存在且 `longTracks = 0` | 116（13.0%） |
| 有 long track 无 CKF dump | 0 |

116 条互斥拒绝原因：`missing_station` 59，`other` 46，`no_truth_match` 9，`fit_failure` 2。`short_segment` 互斥为 0，因为缺站的 59 条同时也是短 segment（独立旗标 `short_segment = 59`）。

`other` 46 条已有四站，仍不是 long track：生产 long-track 选择比“四站 / ≥12 hit”更严。本任务不把该观察写成 cut。

各源拒绝比例约 6%–19%，与一端源冒烟（13/98）一致。

## B6.2 重建契约

当前 transport validation / alignment likelihood 使用：

`raw_CKF_collection` = 全部 `CKFTrackCollection`（只要 5×5 与有限带符号 q/p）

文档中的、**未实现** 的契约：

`eligible_for_transport_validation` = long-track 等价（四站、四 segment、state / 协方差 / q/p / 拟合状态可用）

ntuple `Track_*` 已是 long-track 子集。alignment 没用它，用的是 raw CKF。这就是输入范围错位。

## B6.3 冻结尾巴对照（仅诊断）

未重新筛选。未删除。不得写成“去掉坏事例后 closure PASS”。

| 样本 | N | χ² 均值 | χ² 中位数 | 冻结 1% 残留 | q/p pull RMS |
| --- | --- | --- | --- | --- | --- |
| 原始 official pair | 2352 | 33.33 | 0.150 | 24 | 4.82 |
| long-track 等价 | 2094 | 24.64 | 0.116 | 9 | 1.15 |

15/24 条冻结尾巴随不完整 CKF 离开，均值从 33 降到 25。最大值仍是 \(3.7 \times 10^4\)。剩余 9 行 / 7 个身份：

| 身份 | 含义 |
| --- | --- |
| `100043/37`（3 个 pair） | long track 完整，χ²/ndof=1.42，\|q/p pull\|≈13.8，414 GeV vs 2.0 TeV |
| `100043/13` | long track，CKF χ²/ndof=5.18（拟合差） |
| `100043/50`, `100044/2`, `100044/3`, `100047/80`, `100047/68` | long track，仍在冻结 1% χ² 里，但 q/p pull 不大 |

剩余 `|q/p pull| ≥ 5` 的 3 行全部是 `100043/37`。这是次因 Case B，不是“范围一收就过关”。

## B6.4 15/24 短径迹 / 缺站

| 互斥原因 | N | 代表 |
| --- | --- | --- |
| `missing_station` | 5 | `100044/92`、`100047/40`、`100043/2` 缺站 2+3；`100048/15` 缺站 3 |
| `no_truth_match` | 4 | `100048/86`、`100043/22`、`100043/49` |
| `other` | 6 | `100043/8`、`100048/61`（3 pair）、`100047/47`、`100044/62`：四站且 hit≥19，仍无 long track |

q/p σ 来自 dump Cin，约 \(10^{-7}\)–\(10^{-6}\) / MeV，不是这些大 pull 的原因。

## 机制判定

| Case | 结论 | 依据 |
| --- | --- | --- |
| A 输入契约缺失 | **主因** | 13% unique CKF 不是 long track；15/24 冻结尾巴在这批里。alignment 吃 raw CKF。 |
| B CKF 拟合质量 | **次因** | long-track 对照后仍有 9 条冻结尾巴；`100043/37` 的 q/p 仍错；`100043/13` 拟合 χ²/ndof=5.18。 |
| C 剩余物理 | 未升为主因 | 要等输入范围被定义之后，再单独看 37 这类高动量事例。 |

下一步（允许路线，尚未执行）：

```
WB101
  → Task B6（本 workbook）：Case A
  → 先承认输入范围错位（尚未实现过滤器）
  → 次级：CKF 拟合质量（37 / 13）
  → Transport covariance V3（必须带明确输入范围）
  → Measurement Model V2
  → Alignment estimator
```

禁止为过 χ² gate 去调 C/Q、删尾巴，或把 long-track 对照写成 PASS。

## 工程记录

起始 HEAD：`32c9044a8543845aaa0767d8089ba6fd731f31a9`

Diff（相对 WB101，未提交）：

- `configs/ckf_reconstruction_contract_audit_v1.yaml`
- `datasets/ckf_reconstruction_contract.py`
- `scripts/audit_ckf_reconstruction_contract.py`
- `tests/test_ckf_reconstruction_contract.py`
- `docs/ckf_reconstruction_contract_audit.md`、`docs/ckf_reconstruction_contract_audit_cn.md`
- 本 workbook

Config SHA：`33987dd489b9b9f2ab3f66b772484d28c0da322e6b3579858c4f66bd48f4f9dd`

Run ID：`sbb6_ckf_reconstruction_contract_20260906T191538Z_afbc27fc`

继承 WB101 decision SHA：`92e96c6fcd7bd2d0e580f988cc33814e37050108eb27cf96c6a602dbaab6ec43`  
继承 WB101 provenance SHA：`37479a16db2328b0384a46abf1689db4d6f19ab913fc3a91a9e2acbb0983f82b`  
继承 WB100 decision SHA：`03c277c621feee891cc0608c05a78b2942180ec6918831624304444f18d8f3be`  
继承 WB99 tail SHA：`c6ad96f9b019f0ef1f32ec6d5476ebbb99234aab8e9207a2a314ad89e94627ce`

产物（EOS，不入 git）：`outputs/ckf_reconstruction_contract_audit_v1/sbb6_ckf_reconstruction_contract_20260906T191538Z_afbc27fc/`

| 产物 | SHA256 |
| --- | --- |
| `ckf_input_scope_audit.json` | `13e2e8c19a9767138b8a73fba20f8d3a7e3971de383d3420fae5db546108060c` |
| `ckf_reconstruction_contract.json` | `9bac0c2a5139abf64935ac6e4c521bf9ae80deb70d3156a53d177cf64a47cdd2` |
| `tail_reproduction_audit.json` | `9ee880f6a2f8637a56ed5d2091bba5e5a473f984259ac40443c147ab9f29ca54` |
| `short_track_failure_analysis.json` | `729a4c3ac9c86bfb9704301409e498d04c0294d79cfde74fa052ca0e6d6413ab` |
| `ckf_reconstruction_contract_decision.json` | `5c5936605e4a1ae722c068a75fb35c05f56820714fee37c496d0ca44267b8b94` |
| `inherited_stage.json` | `7fab62375942c43acb02783675e6de7b67bf88100a12bc0ceb0dd6c8bb79ca96` |
| `COMPLETE.json` | `9324f7a2b0444a6bcc50a345b4b2ab269d55daf8de0b8af6d1ba2192e685e285` |

测试：`tests/test_ckf_reconstruction_contract.py`，11 passed。

本审计读已有 dump / ntuple，与 WB99–WB101 同类，在 login 上完成，未另开 HTCondor。

## 下一步

主因 Case A：Transport covariance V3 之前必须先承认——进入 likelihood 的 state 目前来自 raw `CKFTrackCollection`，不是 long-track 契约。本任务没有实现过滤器。

次因 Case B：即使只看 long-track 等价样本，`100043/37` 的 q/p 和若干冻结 χ² 尾巴仍在。不要用缩放协方差去“修”它们。
