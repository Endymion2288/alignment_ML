# Workbook 128: Task B14ZC 已认证 Mean 的共用网格独立导数契约

日期：2026-09-09
状态：**完成 / PASS** —— 以 GitHub `master` `55cf982302a3c62c57b74f368d5e3ba7723fd33a` 为 checkpoint，以 **WB127** 为最新冻结。在**不改变** WB127 已认证 mean semantics（`computeEnergyLossBethe + evaluateMaterialSlab gating + updateState p/E→q/p`）、不改 official accepted-step RKN replay、不改 WB114 measurement likelihood、不改生产 `stepTolerance=1e-4`、不改变正式 `h,h/2,h/4,h/8` FD ladder、不增加 FD rung、不缩小 track-state FD 步长、不放宽冻结 5% gate、不回调 WB124 DOPRI5、不把 WB125 失败的 20/10/5 classical RK4+Mean 当作 official C、不按 Jacobian 符合度挑选网格或材料配置、不加 prior / ridge、不删 `100043/37` 或 `100048/86`、不用 truth q/p、不调 Q / Cin、不修改 `FieldGradientDefaultExtension` 的前提下，只用 WB127 已认证 shadow mean function，为 `100048/86` required hops `1/6, 2/11, 3/11` 重建 common-grid independent central FD。未提交 1989。未进 B15 / Measurement Model V2 / alignment / ML。未覆盖 WB109 / WB114–WB127 / `b14x_smoke`–`b14zb_smoke` dumps。禁止把 target 2/3 earlier hit 6 偷换成 required hop。禁止把真空 ODE 或 WB124 adaptive DOPRI5 称为 reference。

**最终判定：`PASS` / Case `certified_mean_independent_reference_established`**

- `decision = certified_mean_independent_reference_established`
- `primary_case = certified_mean_independent_reference_established`
- `verdict = PASS`
- `smoke_gate_passed = true`
- `control_0_pass = true`
- `control_1_pass = true`
- `control_37_pass = true`
- `mean_path_unchanged = true`
- `target_exclusion_holds = true`（12 行，0 leaked）
- `shadow_mean_contract_established = true`
- `focus_independent_reference_established = true`
- `jacobian_contract_established = true`
- `b14m_reopen_authorized = true`
- `restart_invariance_authorized = false`
- `full_sample_authorized = false`
- `prior_introduced = false`
- `ridge_added = false`
- `statistical_model_unchanged = true`
- `five_percent_gate_unchanged = true`
- `do_not_change_mean_semantics = true`
- `do_not_reuse_wb124_adaptive_dopri5 = true`
- `do_not_submit_1989 = true`
- `b15_authorized = false`
- `measurement_model_v2_authorized = false`
- `next_step = reopen_b14m_smoke_and_restart_invariance`

这是 **Jacobian contract PASS**，也是 B14M smoke 重新打开的授权，**不是** 1989 行正式提交，也不是 restart invariance 已经做完。下一本只能重开 B14M smoke，并重做 nominal / `loc1+1mm` / `phi+1e-3` / `q/p×1.1` restart invariance。本跑没有提交 Condor。

## 起始状态

HEAD：`55cf982302a3c62c57b74f368d5e3ba7723fd33a`

WB127 PASS：`surface_energy_loss_semantics_established`  
Official run：`sbb14zb_surface_eloss_semantics_20260909T161125Z_be4e054a`  
Decision SHA：`aa80751fb532cf36350a8cf78b25f9d74da5bc707b9739ba7b683ef294fe04c4`  
Config SHA：`6b89a65070355d0143561d3017dbd451870ab74a7cbcf5fac77e9e1bd1ba27fa`

```
shadow_mean_contract_established = true
focus_independent_reference_established = false
jacobian_contract_established = false
b14m_reopen_authorized = false
production_eloss_quantity = computeEnergyLossBethe
control 0/1/37 PASS
mean_path_unchanged = true
```

WB123 已冻结且本任务未改：

```
FieldGradientDefaultExtension.hpp SHA
ca5e4f0ef1a7a09edd1c24099e7ce689c4f0511e2d4afab3a160ebd2d6ff03d8
A = repaired production tangent
control 0/1/37 PASS
official mean Δloc0 = 0
```

冻结 likelihood 不得改写：

```
theta = (loc0, loc1, phi, theta, q/p)
alpha = (loc0, theta)
nu    = (loc1, phi, q/p)
chi2(θ) = Σ r_i(θ)^T R_i^{-1} r_i(θ)
R_i = (0.08 mm)² / 12
```

冻结 FD ladder：

```
h(loc0)=0.01 mm, h(loc1)=0.01 mm, h(phi)=1e-5, h(theta)=1e-5, h(q/p)=1e-6 /GeV
rungs: h, h/2, h/4, h/8
last-pair rel ≤ 0.05 and sign consistent
production stepTolerance = 1e-4
```

## 本任务做了什么

只跑 `100048/86` required hops。control `100043/0,1,37` 继承 WB123 冻结 dump，不重跑、不改。

1. **C** = 已认证 shadow mean 的 common-grid independent central FD。网格是 **nominal 生产 accepted-step + 材料表面序列**，由未扰动 hop 冻结；每个 `+δ/−δ` 臂复用同一分区、同一 Bethe 门控、同一 supporting-plane 投影。场更新仍是 official RKN4 replay，不是 classical RK4，不是 WB124 DOPRI5，不是 WB125 20/10/5。
2. **A** = WB123 repaired production tangent（`field_gradient_repair.hop_dloc0_d_start`）。`FieldGradientDefaultExtension` 未改。
3. 认证顺序严格执行：先确认每个 perturbed arm 仍满足同一 mean-function contract 且无 branch / material-node switching；再要求 C 的四档 ladder 自身收敛；最后才比较 A 与 C。
4. `loc1/phi/q/p` 必须全部建立独立 reference。`loc0/theta` 完整记录，但不能代替前三列。

Mean semantics 只被 **调用**，没有被改写。`MeanTransportContract.hpp` 的 Bethe / gating / `updateState` 路径保持 WB127 合同。

## 正式产物

Official run：`sbb14zc_certified_mean_fd_20260909T165606Z_5bd09dfe`

```
config SHA      91bbf89658082979ef92fb1ee56b8f32ee9133cfd091dfe5c01412425d942ab8
decision SHA    7f9d411c31d3eb94d63fb13e24f1ebd57b3095f0007039a5c3048704007141b1
helper SHA      0d58d4cfca59a8de2d798771b3d454757713348955411681854b0073404d32df
mean header SHA 8f9d162e59f590770baba1f0c393a7f10861a85495dd7bfbac676d8b59bd15da
dump SHA        04eab2a75e84e46103c1edae75ed3cf014039a7780cf2919ebb5a3f9cc054631
field-grad SHA  ca5e4f0ef1a7a09edd1c24099e7ce689c4f0511e2d4afab3a160ebd2d6ff03d8
```

Dump：`outputs/leave_target_out_dump_v1/b14zc_smoke/mc24_100048_00000_00049/ckf_leave_target_out_certified_mean_fd.jsonl`（3 行）

Artifacts：

```
certified_shadow_fd_ladder.json
perturbed_arm_mean_contract.json
repaired_tangent_vs_certified_shadow.json
required_segment_derivative_taxonomy.json
certified_mean_common_grid_fd_decision.json
inherited_stage.json
COMPLETE.json
```

`tests/test_certified_mean_common_grid_fd.py`：5 passed。

## 第一步：perturbed arm 仍是同一个 mean function

三个 required hop 上，所有 `+δ/−δ` × 四档：

```
all_arms_same_mean_contract = true
all_branch_identity_same = true
all_material_node_identity_same = true
any_projection_fail = false
```

场步数、材料 geometry ID、`updateState` 旗标与 nominal 一致。没有 adaptive 重新分区，因此 WB124 的 accepted-step 分支噪声不再出现。名义影子 mean 继续通过 WB125/WB127 预注册 gates（与 WB127 同一数字）：

```
T1/6:  Δloc0=-3.07e-7 mm, Δpath=0, Δqop_rel=1.60e-7
T2/11: Δloc0=+1.70e-8 mm, Δpath=0, Δqop_rel=3.19e-8
T3/11: Δloc0= 3.55e-14 mm, Δpath=0, Δqop_rel=1.14e-8
```

官方 mean 在打开 field-gradient repair 后 `Δloc0 = 0`。

## 第二步：C 自身收敛

Certified-shadow FD，`last-pair rel` 和符号：

| hop | loc0 | loc1 | phi | theta | q/p |
|---|---|---|---|---|---|
| T1/6 | 8.9e-5, + | 3.9e-4, − | 7.7e-5, + | 2.1e-5, − | 2.9e-4, − |
| T2/11 | 7.3e-6, + | 2.6e-5, − | 7.4e-4, + | 8.4e-6, − | 2.2e-4, − |
| T3/11 | 7.5e-6, + | 2.0e-6, − | 3.2e-4, + | 8.0e-6, − | 1.5e-4, − |

全部 `converged=true`，`sign_consistent=true`，last-pair ≪ 5%。`loc1/phi/q/p` 独立 reference 成立。`loc0/theta` 同样收敛，但只作记录，不代替前三列。

不允许因为 A 已经稳定就跳过这一步。本跑是 C 先自己过门，再比 A。

## 第三步：A ≈ C（冻结 5% gate）

`A` = WB123 repaired tangent。相对误差 `|A−C|/|C|`：

| hop | loc0 | loc1 | phi | theta | q/p |
|---|---|---|---|---|---|
| T1/6 | 1.14e-4 | 1.09e-4 | 7.5e-7 | 1.03e-6 | **1.20e-2** |
| T2/11 | 1.30e-5 | 9.0e-6 | 6.3e-4 | 3.6e-6 | **8.39e-3** |
| T3/11 | 2.15e-6 | 3.4e-5 | 3.8e-4 | 5.8e-6 | 3.19e-4 |

最差列是 T1 `q/p` 1.20%，仍低于冻结 5% gate，符号一致。三个 required hop 的 taxonomy 都是 `certified_mean_independent_reference_established`。

没有把 WB124 DOPRI5 或 WB125 Mean+RK4 当作 C。没有为了 A≈C 改网格、改材料、改 Eloss。

## 分类

允许的 token 里，本跑是：

```
T1/6  = certified_mean_independent_reference_established
T2/11 = certified_mean_independent_reference_established
T3/11 = certified_mean_independent_reference_established
primary_case = certified_mean_independent_reference_established
```

没有 `field_map_derivative_reference_nonsmooth`。  
没有 `material_update_derivative_discontinuity`。  
没有 `supporting_plane_event_derivative_sensitivity`。  
没有 `numerical_segment_fd_not_contracted`。  
没有 `repaired_variational_focus_inconsistent`。  
没有 `mixed_or_inconclusive`。

Mean 已经在 WB127 认证，本跑不能再把任何残差写成 shadow mean mismatch。实际上也没有这类残差。

## 冻结旗标

```
control 0 PASS = true
control 1 PASS = true
control 37 PASS = true
mean_path_unchanged = true
shadow_mean_contract_established = true
focus_independent_reference_established = true
jacobian_contract_established = true
b14m_reopen_authorized = true
restart_invariance_authorized = false
full_sample_authorized = false
```

即使 Jacobian contract 已建立，也**不得直接提交 1989**。`restart_invariance_authorized` 保持 false，直到下一本把 B14M smoke 和四组 restart 做完。

## 下一任务

主线保持用户给定顺序，现已走完前三步：

```
WB127 mean contract PASS
  → WB128 已用同一个已认证 mean function 建 independent segment FD
           independent reference + repaired tangent PASS
           Jacobian contract 正式建立，B14M smoke 重新打开
  → 下一本: 只重开 B14M smoke
           重做 nominal / loc1+1mm / phi+1e-3 / q/p×1.1 restart invariance
           不要提交 1989
```

`next_step = reopen_b14m_smoke_and_restart_invariance`。不要把本跑误写成 1989 授权，不要改 mean semantics，不要再回调 WB124/WB125 失败配方。
