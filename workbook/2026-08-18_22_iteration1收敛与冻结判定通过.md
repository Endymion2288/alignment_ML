# 2026-08-18 (22) iteration-1 收敛、冻结判定通过

## 事件

- 15:20 左右 iteration-1 Condor 库（cluster 996698）完成：144/144 点文件齐全（tracklets + propagations + content_audit），全部 uproot 可读，无损坏（iteration-0 的 `cov_yy_mm2` 损坏未重现）。
- 运行 iteration-1 closure 聚合（`run_multisource_refit_multidof_local_step.py`，anchor=`iteration_01_anchor`，target=`iteration_01_reference`），输出 `outputs/mc24_multidof_ift_iteration01_anchor_trainval_closure_v1/`。fit 观测 2347 对，held-out 1795 对，rank 3/3。
- 运行 `compare_multidof_iterations.py`（iteration-0 + iteration-1），输出 `outputs/mc24_multidof_ift_iteration_comparison_v1/`（JSON/CSV/趋势图/中文片段）。

## 结果（held-out validation，冻结容差 dx≤0.1 mm、dy≤0.1 mm、Ry≤1 mrad）

| 迭代 | anchor dx/dy/Ry | held-out 偏差 dx/dy/Ry | χ² 下降 | 条件数 | dx-Ry 相关 | 逐源散布 dx/dy/Ry | 链召回(anchor) | 判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| iteration-0 | 2.00/−1.50/35.0 | −0.255/−0.221/−0.05 | 99.6% | 1881 | 0.85 | 0.545/0.332/1.197 | 0.44 | 未通过 |
| iteration-1 | −0.14/+0.11/+0.74 | **−0.014/+0.019/−0.000** | 99.0% | 5328 | 0.57 | **0.013/0.025/0.016** | 0.40 | **通过** |

- iteration-1 的 anchor 即 iteration-0 的 proposed values；在新 anchor 处重测，held-out 独立拟合恢复剩余偏移到几乎为零。
- **逐源散布塌缩 20–40 倍**（dx 0.545→0.013 mm），与诊断一致：远离 nominal 时 dx-Ry 近简并（0.85）放大涨落；接近 nominal 后相关降到 0.57，源间响应一致。
- χ² 下降保持 99%；链召回 0.40–0.44 不变（coverage 问题与对齐收敛正交，机制已归因：mode-0 传播协方差各向异性失校 + x/tx 重尾 + χ² 门）。

## 冻结判定

`compare_multidof_iterations.py` 输出：**`proceed_to_route_selected_update`** — 末次 iteration 通过全部冻结容差，按预先冻结的逻辑进入 frozen association backbone 的 route-selected update（`scripts/run_route_selected_multidof_update.py`，需要 anchor/target/±FD 点的冻结 V1/V2 association 输出）。

## 下一步（待用户确认后启动）

route-selected update 需要对 iteration-1 bank 的 anchor + reference + 6 个 FD probe 点跑冻结 V1/V2 association backbone 推理（仅推理，不训练）。这是新的大规模 GPU 推理生产，按惯例先与用户确认范围（全部 18 源还是 train-only、用 V1 还是 V2 路由）再提交。
