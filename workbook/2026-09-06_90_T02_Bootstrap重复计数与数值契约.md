# Workbook 90: T02 Bootstrap 重复计数与数值契约

日期：2026-09-06
状态：**完成** —— 只修统计/数值契约，不重跑历史 rank campaign，不改写冻结负结果。

**最终判定：`PASS`**

- `rank_tolerance = 0.01`（未改）
- `boolean_mask_used = false`
- `historical_rank_not_reinterpreted = true`
- `geometry_write_allowed = false`

## 1. 科学问题

`bootstrap_subspaces` 把有放回抽样转成布尔 mask，丢掉 event multiplicity。
欠定 SVD 在 `m<n` 时丢失 extra right-null。非 SPD 协方差可以给出负 chi2。

## 2. 修复

- 事件键 = `(source UID, run, event)`；强制抽取 `[event0, event0, event1]` 时
  normal 贡献等于 `2 N0 + N1`
- 同源不同 source 的相同 run/event 不合并
- `m<n` 时 `dim(V_null)=n-rank`，且 `A V_null ≈ 0`
- `[[1,2],[2,1]]` 被拒绝，不能返回 `chi2=-2`

历史 workbook 68/69/73 的 rank 数字保持原样，不因此变成 deployment pass。

## 3. 产物

Run ID：`t02_bootstrap_contract_20260906T125645Z_ffe42699`

Config SHA：`416a34080f2bd91811f967c9b05c079d72622b3a1eb54da47d8e1e26989de300`

回归产物 SHA：`344b27cb88f03a791bd86c07f6ddfeed4a700587b6432be02463dc527904c266`
