# Workbook 101: Task B5 CKF tail provenance / association 审计

日期：2026-09-06
状态：**完成** —— 冻结尾巴的错误 q/p 状态已溯源。未修 closure，未删尾巴，未设计 cut，未调 C/Q，未进入 Measurement Model V2 / alignment / ML。未改 WB87–WB100 结论。

**最终判定：`AUDITED`（主因 Case B）**

- `decision = ckf_tail_provenance_audited`
- `primary_case = ckf_reconstruction_failure`
- `secondary_cases = wrong_association_or_identity`
- `next_step = tracking_reconstruction_audit`
- `closure_pass = false`
- `measurement_model_v2_entered = false`

冻结保持：

- WB96：`ckf_qoverp_covariance_export_established`
- WB98：`acts_q_materialized_closure_failed`
- WB99：`high_chi2_tail_dominated`
- WB100：`wrong_track_state_dominated`

## 起始状态

HEAD（任务开始时）：`32c9044a8543845aaa0767d8089ba6fd731f31a9`

相对 WB100：只新增本任务文件。未覆盖 WB98 dumps、WB99/WB100 产物。名单是 WB99 official pair top 1%（24）/ 0.1%（3），与 WB100 分类 keys 一致，未重新筛选。

## 验收标准

| 项 | 要求 | 结果 |
| --- | --- | --- |
| B5.1 溯源 | `ckf_tail_provenance.json` | **完成**。24/24、3/3 保留。 |
| B5.2 q/p | truth 仅诊断 | **完成**。未写入重建。 |
| B5.3 association | 多 candidate / 跨 pair | **完成**。无重复 CKF candidate；4 个身份同一 state 传到多 surface。 |
| B5.4 质量变量 | 不据此做 cut | **完成**。smoothing / outlier / material 字段为 null。 |
| 分类 | Case A / B / C | **Case B**。次因 A。 |
| 禁止项 | 不进 V2，不调 C/Q | 保持。 |

## B5.1 CKF state provenance

每个尾巴行对应 `CKFTrackCollection` 的 `track_index=0`。dump 里每个 event **只有 1 条** CKF 径迹，不是多 candidate 打架。

关键分裂：

| 现象 | 冻结 1%（24 行） |
| --- | --- |
| CKF 在 dump 中存在，但 ntuple `longTracks=0` | 15 |
| 同一 state 出现在多个 station pair | 10（4 个身份：37、22、15、61） |
| 缺站（long-track `InStation` 不全） | 6 |
| segment 数 < 4 | 5 |
| CKF χ²/ndof ≥ 4 | 1 |

alignment 输入用的是 **全部 CKFTrackCollection**。标准 ntuple 的 `Track_*` 是通过 long-track 选择后的子集。15/24 条尾巴是“CKF 有、long track 没有”的状态：这些 state 被送进 transport / χ²，但不是实验通常承认的长径迹。

## B5.2 q/p failure（仅诊断）

| 量 | 冻结 1% |
| --- | --- |
| \|q/p pull\| ≥ 5 | 16/24 |
| 十年错误 p | 6/24 |
| q/p pull RMS | 33.7 |
| truth q/p 进入重建 | false |

0.1% 三条都有 \|q/p pull\| ≥ 5，但来源不同：

| event | 主类 | 含义 |
| --- | --- | --- |
| 100043/37 | C（剩余物理） | long track 完整，χ²/ndof=1.42，4 站 TMF=1，但 reco 414 GeV vs truth 2.0 TeV |
| 100048/86 | A | `longTracks=0`，station 0 tracklet truth id = −1 |
| 100044/92 | B | `longTracks=0`，只有 2 个 segment |

## B5.3 Candidate / association

- 重复 CKF candidate：0
- 冲突 tracklet truth id：3 行
- 未匹配 tracklet（id≤0）：3 行
- 低 TMF：3 行
- Case A 主类：7/24（29%）

跨 pair 的 4 个身份都是 **同一 `track_index=0` 被传播到多个目标面**，不是不同 candidate。这解释了 WB100 的 repeated-event 现象。

## B5.4 Quality variables

有 long track 可 join 时：n measurements、n layers、χ²、ndof、`InStation*`、hitSet 已记录。ntuple **没有** smoothing status、outlier measurements、material interaction 计数，这些字段为 null。未根据分布设计 cut。

## 机制判定

| Case | 结论 | 依据 |
| --- | --- | --- |
| A 错误 association | **次因**（29%） | 未匹配 / 冲突 truth id / 低 TMF。不是主因。无重复 CKF candidate。 |
| B CKF 重建失败 | **主因**（58%） | 15/24 条 CKF 不是 long track；另有缺站、少 segment、1 条坏拟合。输入 alignment 的 collection 比 long-track 选择更宽。 |
| C 剩余物理 | 3/24 | 主要是 100043/37：关联与拟合都合格，q/p 仍错。这是高动量 q/p 可测性，不是 covariance 调参对象。 |

下一步（允许路线，尚未执行）：

```
WB100
  → Task B5 (本 workbook)：Case B
  → tracking reconstruction audit
     （CKFTrackCollection vs long-track 选择；缺站 / 短径迹）
  → 次级：association QC（未匹配 tracklet）
  → 再评估剩余高动量 q/p 物理
  → Transport covariance V3
  → Measurement Model V2
```

禁止为过 χ² gate 去调 C/Q 或删尾巴。

## 工程记录

起始 HEAD：`32c9044a8543845aaa0767d8089ba6fd731f31a9`

Diff（相对 WB100，未提交）：

- `configs/ckf_tail_provenance_v1.yaml`
- `datasets/ckf_tail_provenance.py`
- `scripts/audit_ckf_tail_provenance.py`
- `tests/test_ckf_tail_provenance.py`
- `docs/ckf_tail_provenance_analysis.md`、`docs/ckf_tail_provenance_analysis_cn.md`
- 本 workbook

Config SHA：`9489dcc0a5dea3c3c07ac313376616a27cc5e62000fde67482a13090bd264cac`

Run ID：`sbb5_ckf_tail_provenance_20260906T183700Z_f95fb941`

继承 WB100 decision SHA：`03c277c621feee891cc0608c05a78b2942180ec6918831624304444f18d8f3be`  
继承 WB100 classification SHA：`4b87eeb9125942f6d6f1b535bc464b38756c50d0176ee3a775ea9aa5b71adf14`  
继承 WB99 tail SHA：`c6ad96f9b019f0ef1f32ec6d5476ebbb99234aab8e9207a2a314ad89e94627ce`

产物（EOS，不入 git）：`outputs/ckf_tail_provenance_analysis_v1/sbb5_ckf_tail_provenance_20260906T183700Z_f95fb941/`

| 产物 | SHA256 |
| --- | --- |
| `ckf_tail_provenance.json` | `37479a16db2328b0384a46abf1689db4d6f19ab913fc3a91a9e2acbb0983f82b` |
| `qoverp_failure.json` | `45dd5cd0e6ba6e027113a7530c7458ee0a35b3563e77d14fb6da0a5cac6121c8` |
| `association_consistency.json` | `597c2e6dfc00909c403a3c416e5c055830a24b478299bf3f7f21e85190a24df4` |
| `ckf_tail_provenance_contract.json` | `92e96c6fcd7bd2d0e580f988cc33814e37050108eb27cf96c6a602dbaab6ec43` |
| `inherited_stage.json` | `c5240040fab09756dfc45a4e4cabc31acece5322195b655f879a53d509d6cc69` |
| `COMPLETE.json` | `baf11a99ed2ba7e437b85e49e926a1b35ebefcbecf4be834752cf60c3dd19981` |

测试：`tests/test_ckf_tail_provenance.py`，8 passed。

## 下一步

主因 Case B：先审计 **CKFTrackCollection 为何包含未通过 long-track 选择的状态**，以及缺站 / 短 segment 径迹。不要调 covariance。

次因 Case A：未匹配或冲突的 tracklet truth id，可并行做 association QC。
