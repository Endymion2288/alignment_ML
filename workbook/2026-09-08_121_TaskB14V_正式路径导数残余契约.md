# Workbook 121: Task B14V Official-Path Derivative Residual Contract Resolution

日期：2026-09-08
状态：**完成 / FAIL** —— 以 GitHub `master` `55cf982302a3c62c57b74f368d5e3ba7723fd33a` 为 checkpoint，以 **WB120** 为最新冻结。在**不改变** WB114 measurement likelihood、不重调 Gauss–Newton、不改生产 `stepTolerance=1e-4`、不改变正式 `h,h/2,h/4,h/8` FD ladder、不放宽冻结 5% gate、不按结果挑最好 FD 步长或积分精度、不把正式 sequential likelihood 换成 direct-from-source、不用任何 ACTS Jacobian 替换正式 likelihood、不加 prior / ridge、不删 `100043/37` 或 `100048/86`、不用 truth q/p、不以修 5D Cin 为目标、**不再换 Jacobian 实现**的前提下，只对预注册 control `100043/0,1,37` 与 focus `100048/86` 分类 WB120 留下的两处残余矛盾。未重开 B14M，未做 restart invariance，未进 B15，未进 V4 C/D，未进 Measurement Model V2。未覆盖 WB109 / WB114 / WB115 / WB116 / WB117 / WB118 / WB119 / WB120 dumps。未重跑 Athena。**未提交 1989 行 Condor**。禁止把 dummy-cov bounded `transportJacobian` 当作正式导数。

**最终判定：`FAIL`**

- `decision = mixed_or_inconclusive`
- `primary_case = mixed_or_inconclusive`
- `smoke_gate_passed = true`
- `jacobian_contract_established = false`
- `b14m_reopen_authorized = false`
- `restart_invariance_authorized = false`
- `full_sample_authorized = false`
- `free_state_jacobian_available = true`
- `official_loc0_matches_diagnostic = true`
- `control_fd_converged = true`
- `control_five_percent_pass = false`
- `focus_fd_converged = false`
- `focus_five_percent_pass = false`
- `segment_composition_closed = true`
- `step_level_D_persisted_in_acts = false`
- `higher_precision_variational_state_available = false`
- `branch_switching = false`
- `target_exclusion_holds = true`（12 行，0 leaked）
- `prior_introduced = false`
- `ridge_added = false`
- `statistical_model_unchanged = true`
- `five_percent_gate_unchanged = true`
- `do_not_relax_five_percent_gate = true`
- `do_not_change_official_fd_ladder = true`
- `do_not_change_jacobian_implementation = true`
- `do_not_use_dummy_cov_bounded_transportJacobian = true`
- `b15_authorized = false`
- `measurement_model_v2_authorized = false`
- `active_residual_kinds = official_path_derivative_still_inconsistent, fd_reference_not_precise_enough_for_derivative_certification`
- `next_step = keep_residual_diagnosis_without_shrinking_fd`

这仍然不是 B14M PASS。WB120 已经拿到正确同路径 Jacobian。本任务证明：control `100043/1` 的 `loc1` 不是小列 relative-gate 假阳性，而是相对已收敛 FD 的真实 ~6% 局部导数差；86 的四档 FD 本身不够精确，analytic 与 FD 的偏差与 FD 层间差同量级。两类残余同时存在，因此只能判 `mixed_or_inconclusive`。5% gate 未改。未重开 B14M。未提交 1989。

不允许把本跑判成 `official_path_derivative_contract_established`：control 1 与 86 都未过冻结 5%。不允许把 control 1 判成 `small_column_relative_metric_pathology`：`|J_a-J_FD|` 约是 last-pair envelope 的 90 倍。不允许把 86 单独说成“继续缩步长就能认证”：下一步应建立独立 derivative reference。不允许改生产 `stepTolerance`、减小 FD 步长、或把 ACTS 矩阵写进正式 likelihood。

## 起始状态

HEAD：`55cf982302a3c62c57b74f368d5e3ba7723fd33a`

WB120 FAIL：`official_path_jacobian_inconsistent_with_fd`  
Official run：`sbb14u_official_jacobian_20260908T184910Z_d5148ffc`  
Decision SHA：`ad63a600d3b2ec4c10744f73719e58f87ba87cbf7d77852d292c4a3997421a01`  
Config SHA：`7aeaa12fbb09d0b2ae7b26976b0cbb3978ae748e110380ede539cafe75e42b00`  
Helper SHA：`bea305738d479f335fd64b1f7a92e0d9daf66a395873d17dda18bfa2e19927c3`

```
decision = official_path_jacobian_inconsistent_with_fd
jacobian_contract_established = false
b14m_reopen_authorized = false
free_state_jacobian_available = true
official_loc0_matches_diagnostic = true
control_fd_converged = true
control_official_path_agrees_fd = false
focus_fd_converged = false
next_step = keep_official_path_jacobian_diagnosis
```

WB120 已证明：正式无边界 supporting-plane 路径的 `rk_free` Jacobian 可获得，函数值与正式 Mode-B predicted loc0 差为 0，`100043/0` 与 `100043/37` 五列通过（含约 3 m 磁 hop 的 q/p）。不再怀疑 Jacobian 路径整体错误。

冻结 likelihood 不得改写：

```
theta = (loc0, loc1, phi, theta, q/p)
alpha = (loc0, theta)
nu    = (loc1, phi, q/p)
chi2(θ) = Σ r_i(θ)^T R_i^{-1} r_i(θ)
R_i = (0.08 mm)² / 12
```

WB103 冻结 denominator 保持：raw 2680 / ineligible 691 / contracted 1989 / official pairs 1974。

冻结 FD ladder 保持：

```
h(loc0)=0.01 mm, h(loc1)=0.01 mm, h(phi)=1e-5, h(theta)=1e-5, h(q/p)=1e-6 /GeV
rungs: h, h/2, h/4, h/8
last-pair rel ≤ 0.05 and sign consistent
```

## 1. 本任务唯一问题

分类两个残余矛盾，不换 Jacobian：

1. `100043/1` target 1/2 的 `loc1` 列 norm ≈ 0.085，`rk_free vs FD` 相对误差 6.16% / 5.64%。是小列 relative-error gate 的数值条件问题、FD 截断/积分误差，还是局部 derivative 真正不一致？
2. `100048/86` 的 `rk_free` 已与 FD 同量级（尤其 q/p 从 WB119 的数量级错误修成 O(10³) 对 O(10³)），但 FD ladder 仍不收敛。必须建立**不挑最好步长**的 consistency diagnosis。

预注册 consistency envelope：`||J_h/4 - J_h/8||`。只用于判断 analytic-vs-FD 是否落在 FD 自身不确定度内。禁止据此修改 5% gate 或挑选某档 FD。

范围严格是预注册 `100043/0,1,37` 与 `100048/86`。不是 44。不是 1989。

## 2. ACTS 32.0.2 一步级 D / variational 自检

EigenStepper（AthenaExternals 24.0.41 / ACTS 32.0.2）：

- 每步接受后：`if (covTransport) jacTransport = D * jacTransport`
- `D` 来自 `GenericDefaultExtension::transportMatrix`，计算后立即乘入，**不保存**一步级 D
- 没有更高精度 variational state API（double only）
- 正式 `nullopt` 路径仍不填 `jacTransport`；WB120 诊断开关已经读出 hop 级积

因此本任务**不**增加新的 C++ Jacobian 实现，也**不**重跑 Athena。允许的内部 self-check 是对冻结 WB120 hop 矩阵做 composition：

```
jRkFreeAcc * boundToFree(start)  ==  bound_to_free_rk_product
jacTransport * jacToGlobal * jacobian  ==  acts_composed
official loc0 - diagnostic loc0  ==  0
```

事件 1 与 86 上这些相对误差均为 0。`segment_composition_closed = true`。物质面 curvilinear 重置仍与正式 `rk_free` 积分离。不手写磁场解析模型。

## 3. 实现

Config：`configs/official_path_derivative_residual_v1.yaml`  
SHA：`3ac25711142694bc3e4eaede110c5653fe6abc2d2ca2ac0552ef1e2d5238a287`

输入是冻结 WB120 smoke，哈希必须匹配：

| dump | bytes | SHA |
| --- | --- | --- |
| `b14u_smoke/mc24_100043_00400_00499/ckf_leave_target_out_official_jacobian.jsonl` | 2241304 | `1c2daf7fda4e585d62624d1705934c3d621a69d560881f3d417d8a96bb1343ab` |
| `b14u_smoke/mc24_100048_00000_00049/ckf_leave_target_out_official_jacobian.jsonl` | 732933 | `831a2a3fc9ff6e0aa519895d642a5de576c42a3a947700fab48df58bb798a19d` |

每列额外记录：absolute difference、relative difference、column norm、FD successive-difference、analytic column norm、analytic vs `h,h/2,h/4,h/8` 每一级、last-pair envelope、residual mismatch / measurement sigma。5% gate 只作冻结对照，不作自动放宽。

Helper SHA 未改：`bea305738d479f335fd64b1f7a92e0d9daf66a395873d17dda18bfa2e19927c3`

测试：`tests/test_official_path_derivative_residual.py` 6 passed（login，无 Athena）。分类器只允许五类判决；禁止 prior / ridge / 换 likelihood / 挑最好步长 / 放宽 5% / 换 Jacobian / dummy-cov bounded Jacobian / 重开 B14M / 提交 1989。

## 4. Smoke（login only：复用 WB120 dumps）

未覆盖 WB109 official dump（仍 1951622 bytes）  
未覆盖 WB120 smoke（上表哈希不变）  
未覆盖 WB117 / WB118 / WB119 dumps  

12 行正式 Mode B 仍是 `100043/0,1,37` 与 `100048/86` × target 1/2/3。

## 5. 逐项结果

### 5.1 Control 0 / 37 — 冻结 5% 仍通过

全部 5 列 × 3 个 target：`residual_kind = frozen_five_percent_pass`。与 WB120 一致。有些列 `|J_a-J_h/8|` 大于极小的 last-pair envelope（FD 已收敛到 1e-6 量级），但相对误差 ≪ 0.05。这不是放宽 gate，也不是 pathology。

### 5.2 Control `100043/1` loc1 — 真实局部不一致，不是 metric 病态

| target | last-pair rel | \|\|Ja\|\| | \|\|Jh\|\| | \|\|Ja-Jh\|\| | rel vs h | envelope | inside env | dres/σ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 6.78e-4 | 0.08485 | 0.08519 | 0.005249 | **0.06161 FAIL** | 5.78e-5 | false | 0.00227 |
| 2 | 6.68e-4 | 0.08485 | 0.08513 | 0.00480 | **0.05638 FAIL** | 5.68e-5 | false | 0.00208 |
| 3 | 3.82e-4 | 0.08485 | 0.08495 | 0.00232 | 0.02728 PASS | 3.25e-5 | false | 0.00100 |

FD 四档彼此稳定（不是截断/积分误差）。analytic 列在 18 个 hit 上严格是立体投影 `±0.02`（`0.02 * sqrt(18) = 0.08485`）。正式 FD 前 6 个 hit 也是 `±0.02`，随后出现 0.001–0.002 的额外耦合。`|J_a-J_FD|` ≈ 90 × envelope，因此**不是** `small_column_relative_metric_pathology`。残差效应只有 strip sigma 的 0.2%，但这不能自动改 gate。`residual_kind = official_path_derivative_still_inconsistent`。

### 5.3 Focus 86 — FD reference 不够精确

target 1 最后一对相对误差仍约：loc0 0.191、loc1 6.04、phi 1.36、theta 0.311、q/p 1.32。`rk_free` 与 FD 同量级。多数列 `|J_a-J_h/8|` 落在 last-pair envelope 内，或与 FD 层间差同量级（预注册 same-order factor = 3）。analytic 不是沿 FD 序列单调趋近的极限（approach 不单调），但 FD 自身在四档之间大幅摆动，包括 q/p 在 `h/8` 上变号。

因此 86 判 `fd_reference_not_precise_enough_for_derivative_certification`。**禁止**继续缩 FD 步长。下一步应建立独立 derivative reference。

86 target 1 的 loc0 相对误差 0.013，target 3 的 loc0 相对误差 0.019，这两列通过冻结 5%；这不能单独认证 86。

### 5.4 Hop composition / ACTS D

事件 0/1/37/86 全部 12 个 target：`max_rk_product_relative_error = 0`，`max_acts_composed_relative_error = 0`，`official_minus_diagnostic_loc0 = 0`。约一半 hop 有物质面 reset，collector 积与 last-reset `jacTransport` 按设计分离。ACTS 不暴露一步级 D，也不暴露更高精度 variational state。

### 5.5 不允许的读法

1. **不是** `official_path_derivative_contract_established`。control 1 与 86 未过冻结 5%。
2. **不是** `small_column_relative_metric_pathology`。control 1 的绝对差远大于 FD self-difference。
3. **不是**可以继续缩 FD 步长来认证 86。
4. **不是**可以把 ACTS 矩阵或 WB119 `transportJacobian` 写进正式 likelihood。
5. **不是**物理不可微，也不是该改生产 `stepTolerance`。

## 6. 正式审计

Official run：`sbb14v_official_residual_20260908T194821Z_c5b463f5`

| 文件 | SHA |
| --- | --- |
| `official_path_derivative_residual_decision.json` | `60d0ced8355b15c98f9758974299b0251b7500d8514fcffd966373bd5b780b44` |
| `official_path_derivative_envelope.json` | `4012497ce3afb427deda17ae3d0d4b6fa7b6f737b2e07ede2fcffeca2078c273` |
| `official_path_hop_self_check.json` | `a3d37ba67cd12864160be3496ee6b66ed1440926a44d0cfbb800fd40ab4911fe` |
| config | `3ac25711142694bc3e4eaede110c5653fe6abc2d2ca2ac0552ef1e2d5238a287` |
| helper | `bea305738d479f335fd64b1f7a92e0d9daf66a395873d17dda18bfa2e19927c3` |

`smoke_gate_passed = true` 只表示冻结 WB120 材料与 envelope / hop 诊断齐全，**不**表示 derivative contract 通过。

## 7. 冻结

```
decision = mixed_or_inconclusive
jacobian_contract_established = false
b14m_reopen_authorized = false
restart_invariance_authorized = false
full_sample_authorized = false
b15_authorized = false
measurement_model_v2_authorized = false
free_state_jacobian_available = true
official_loc0_matches_diagnostic = true
segment_composition_closed = true
five_percent_gate_unchanged = true
do_not_relax_five_percent_gate = true
do_not_change_official_fd_ladder = true
do_not_change_jacobian_implementation = true
do_not_replace_official_likelihood = true
do_not_use_dummy_cov_bounded_transportJacobian = true
do_not_select_best_step = true
do_not_select_best_tolerance = true
statistical_model_unchanged = true
active_residual_kinds =
  official_path_derivative_still_inconsistent
  fd_reference_not_precise_enough_for_derivative_certification
next_step = keep_residual_diagnosis_without_shrinking_fd
```

主线仍然是：

```
WB119 拿错了 Jacobian
  → WB120 给正式 supporting-plane likelihood 建立同路径 derivative
  → WB121 判断剩余 mismatch 是 FD 不够精确、small-column 病态，还是 analytic 仍有真实错误
      ← 本任务：control 1 loc1 = 真实局部错误；86 = FD reference 不够精确
  → derivative contract PASS（control 0/1/37 + 86）   ← 未达到
  → reopen B14M smoke
  → restart invariance
  → only then consider full-sample profile campaign
```

下一步仍停在正式路径 derivative 残余：诊断 control 1 `loc1` 为何缺少 FD 所见的小传输耦合，并为 86 **建立独立 derivative reference**，而不是缩 FD 步长。不允许改生产 `stepTolerance`、放宽 5% gate、换 Jacobian 实现、或把 ACTS 矩阵直接写进正式 likelihood。不重开 B14M，不提交 1989。
