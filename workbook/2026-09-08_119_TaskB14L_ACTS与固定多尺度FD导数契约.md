# Workbook 119: Task B14L ACTS Transport Jacobian vs Fixed Multi-Scale FD Derivative Contract

日期：2026-09-08
状态：**完成 / FAIL** —— 以 GitHub `master` `55cf982302a3c62c57b74f368d5e3ba7723fd33a` 为 checkpoint，以 **WB118** 为最新冻结。在**不改变** WB114 measurement likelihood、不重调 Gauss–Newton、不改生产 `stepTolerance=1e-4`、不按结果挑最好 FD 步长或积分精度、不把正式 sequential likelihood 换成 direct-from-source、不用 ACTS Jacobian 替换正式 likelihood、不加 prior / ridge、不删 `100043/37` 或 `100048/86`、不用 truth q/p、不以修 5D Cin 为目标的前提下，只对预注册 control `100043/0,1,37` 与 focus `100048/86` 做 ACTS-chain vs 固定 FD 阶梯的 derivative contract。未重开 B14M，未做 restart invariance，未进 B15，未进 V4 C/D，未进 Measurement Model V2。未覆盖 WB109 / WB114 / WB115 / WB116 / WB117 / WB118 dumps。**未提交 1989 行 Condor**。

**最终判定：`FAIL`**

- `decision = analytic_chain_or_chart_contract_broken`
- `primary_case = analytic_chain_or_chart_contract_broken`
- `smoke_gate_passed = true`
- `jacobian_contract_established = false`
- `b14m_reopen_authorized = false`
- `restart_invariance_authorized = false`
- `full_sample_authorized = false`
- `control_fd_converged = true`
- `control_acts_chain_agrees_fd = false`
- `focus_acts_chain_stable = true`
- `focus_fd_converged = false`
- `focus_acts_agrees_fd = false`
- `acts_end_loc0_matches_official = true`
- `chain_complete = false`
- `branch_switching = false`
- `target_exclusion_holds = true`（12 行，0 leaked）
- `prior_introduced = false`
- `ridge_added = false`
- `statistical_model_unchanged = true`
- `profile_math_rewritten = false`
- `do_not_select_best_step = true`
- `do_not_select_best_tolerance = true`
- `do_not_switch_to_direct = true`
- `do_not_replace_official_likelihood = true`
- `five_d_cin_not_the_objective = true`
- `wb117_not_treated_as_physical_nonsmoothness = true`
- `wb118_not_a_physical_conclusion = true`
- `b15_authorized = false`
- `measurement_model_v2_authorized = false`
- `next_step = diagnose_jacobian_chain_chart_units_or_projection`

这仍然不是 B14M PASS。WB117 的 `source_to_measurement_transport_not_smooth` 和 WB118 的 `mixed_or_inconclusive` **都不是**物理不可微结论。本任务验证的是 **derivative implementation contract**，不是重新选择一个更好看的有限差分或积分设置。

不允许把 86 判成 `finite_difference_not_reliable_for_focus_transport`：0/1/37 的 FD 阶梯收敛，但同一条 ACTS-chain 已经和冻结 FD 不一致。也不允许把 86 判成 `acts_transport_jacobian_not_numerically_converged`：能写出的 ACTS 列在 `1e-4 / 1e-5 / 1e-6` 上稳定。优先判决是 chart / 链式契约破裂。

## 起始状态

HEAD：`55cf982302a3c62c57b74f368d5e3ba7723fd33a`  
未回到 `32c904...` 工作流。

WB109 PASS：`leave_target_out_state_materialization_established`  
Decision SHA：`af7ccd055c2ff3d34f92429e37be19a89ed90a9b0db11ec6770c161c79b6e933`

WB114 FAIL：`profile_likelihood_numerically_unstable`  
Decision SHA：`82304f5af943f97adfe069e02c8bacdd7491cca72e6ecc58ea82cc470428a18f`

WB115 FAIL：`profile_transport_contract_broken`  
Decision SHA：`dcf6ce865d0c1788e44d0a5a37588c7df3e4b90d31f1c31f67090155a9436c69`

WB116 FAIL：`mixed_or_inconclusive`  
Decision SHA：`3793b0e1a6355717fba567b58fddd406d23503ba312e3d5422a90b82fba6d720`

WB117 FAIL：`source_to_measurement_transport_not_smooth`  
Official run：`sbb14j_jacobian_continuity_20260908T150238Z_a30b79e2`  
Decision SHA：`6a82482ac06e944c8af638099d693738e4c8d7712c1571cabde77e07039b8bf7`

WB118 FAIL：`mixed_or_inconclusive`  
Official run：`sbb14k_map_smoothness_20260908T163845Z_2cbd5d0e`  
Decision SHA：`e22b5d9ce93106b779e968202b84e036c86dc9033639fe4bf833d8245752895d`  
Config SHA：`b0277401ae2a36bdeb453a72902dc7fb7908cc0d8b5c1278bf8db1253b97560e`  
Helper SHA：`078a45ac774971032fd1e4997cb04d2285b66f9fe6b80000f47f580ea0612b33`

WB118 已排除：确定性失败、支撑平面投影病态、FD 信号低于噪声、单纯把 ACTS Jacobian 存在写成 inconsistency。冻结：

```
decision = mixed_or_inconclusive
jacobian_contract_established = false
b14m_reopen_authorized = false
acts_transport_jacobian_available = true
analytic_vs_fd_not_yet_contracted = true
next_step = open_wb119_analytic_vs_fd_derivative_contract
```

冻结 likelihood 不得改写：

```
theta = (loc0, loc1, phi, theta, q/p)
alpha = (loc0, theta)
nu    = (loc1, phi, q/p)
chi2(θ) = Σ r_i(θ)^T R_i^{-1} r_i(θ)
chi2_prof(α) = min_ν chi2(α, ν)
R_i = (0.08 mm)² / 12
```

WB103 冻结 denominator 保持：raw 2680 / ineligible 691 / contracted 1989 / official pairs 1974。

## 1. 本任务唯一问题

ACTS 32.0.2 已提供的 `transportJacobian`，能不能为冻结的 `h_i(θ)` 建立稳定 derivative contract？

范围严格是预注册 `100043/0,1,37`（已知 FD PASS control）与 `100048/86`（focus）。不是 44。不是 1989。

必须把 ACTS bound-to-bound Jacobian **链式转换**到当前 residual chart，不能只拿一个 5×5 和 residual FD 列比。

## 2. 固定阶梯与预注册精度

固定中心步长保持 WB115–WB118：

```
h(loc0) = 0.01 mm
h(loc1) = 0.01 mm
h(phi)  = 1e-5
h(theta)= 1e-5
h(q/p)  = 1e-6 /GeV
```

每列固定记录 `h, h/2, h/4, h/8`。收敛合同：最后一对相对误差 ≤ 0.05 且符号一致。**不**挑最好步长。

积分精度只作 falsification，预注册三档，字段是 ACTS 32.0.2 官方 `PropagatorPlainOptions::stepTolerance`：

```
nominal        1e-4
tighter        1e-5
tighter_again  1e-6
```

**不**按 Jacobian 好看程度挑选生产 tolerance。正式生产仍是 `1e-4`。

ACTS 内部 bound 是 6D（含 time）。契约只用前 5 列 `loc0/loc1/phi/theta/q/p`。q/p 单位：ACTS 内部与 FD 都是 `1/GeV`。

## 3. 实现

Config：`configs/acts_fd_derivative_contract_v1.yaml`  
SHA：`da58a62fc0378976bce0401977b3388015d62e2964daa716d47c2ff31322fc86`

Helper 新开关 `EnableDerivativeContract` 默认关闭。打开后只写正式 Mode B，跳过额外 `jacobian_validation` / B14J / B14K。官方生产路径仍用 `nullopt` covariance；dummy 1e-8 协方差只为取出 `transportJacobian`。

链式记录每一层：

```
source bound
  → ACTS bound-to-bound transportJacobian          (6×6，契约读前 5 列)
  → Surface::boundToFreeJacobian at ACTS end       (8×6)
  → supporting-plane d loc0 / d free               (1×8)
  → intersection free Jacobian                     (8×8)
  → freeToBound at intersection                    (6×8)
  → continuation = f2b * intersection * b2f * J_acts
  → residual column = - d loc0 / d θ_source
```

两条残差列家族：

- `acts_bound_chain`：`e0^T (J_n ⋯ J_0)`
- `projection_composed_chain`：官方残差 chart；**与 FD 的主比较**

Helper SHA：`8edc0d52378263b08ccefa8d87b419992e945507451d9238dc1173aa1828866c`

测试：`tests/test_acts_fd_derivative_contract.py` 5 passed（login，无 Athena）。分类器禁止 prior / ridge / 换 likelihood / 挑最好步长或 tolerance / 重开 B14M / 提交 1989。

## 4. Smoke（login only：0/1/37 + 86）

新 root：`outputs/leave_target_out_dump_v1/b14l_smoke`  
未覆盖 WB109 official dump（仍 1951622 bytes）  
未覆盖 WB117 smoke（仍 17195553 / 5803192 bytes）  
未覆盖 WB118 smoke（仍 4371700 bytes，SHA `a5612a96520baaf2675187292e65a5ef72ac740211b2d561a317521f35a7a810`）

| dump | bytes | lines | SHA |
| --- | --- | --- | --- |
| `b14l_smoke/mc24_100043_00400_00499/ckf_leave_target_out_derivative_contract.jsonl` | 3598319 | 9 | `e8c49c744cea08e8a9f07fbc48417d5cb2a042d8a0c9908f5a3b55e779407ba8` |
| `b14l_smoke/mc24_100048_00000_00049/ckf_leave_target_out_derivative_contract.jsonl` | 1282426 | 3 | `df67bdf16711779b8befccd83238fb1e7d11b539448f8057861e2b34912dc947` |

12 行正式 Mode B：`100043/0,1,37` 与 `100048/86`，各 target 1/2/3。Athena 报告 100043 `FPE OVERFLOWs: 9`、100048 `FPE OVERFLOWs: 3`，12 行仍完整写出。

## 5. 逐项结果

### 5.1 Control FD 阶梯 — PASS

`100043/0,1,37` 全部 5 列 `loc0/loc1/phi/theta/q/p` 在冻结 `h…h/8` 上收敛（最后一对相对误差 ≪ 0.05，符号一致）。这与 WB117 一致，不是重新挑选步长。

### 5.2 Focus 86 FD 阶梯 — FAIL

`100048/86` 五列都不收敛。target 1 最后一对相对误差约：

| 参数 | last-pair rel | official column norm |
| --- | --- | --- |
| loc0 | 0.191 | 4.16 |
| loc1 | 6.04 | 0.660 |
| phi | 1.36 | 229 |
| theta | 0.311 | 7872 |
| q/p | 1.32 | 3933 |

与 WB117/WB118 同一 focus 行为。**不**据此减小 FD 步长。

### 5.3 0-step 几何 hop：chart / 单位 / 投影 — PASS

从 source 出发、官方 `n_steps = 0` 的 hop，ACTS-chain 与正式 FD 在 `loc0/loc1/phi/theta` 上相对误差 1e-9–1e-13。例：`100043/0` t1 meas 2（path 5.515 mm）与 `100048/86` t1 meas 2（path 5.516 mm）。

这证明：

```
J_residual = -dh/dθ
前 5 列（不含 time）
q/p 单位 1/GeV
支撑平面 loc0 = local.x()
J_proj * J_b2f_end * J_acts
```

都不是 0/1/37 失败的原因。ACTS 已落在面上时，`projection_composed` 与 `acts_bound` 相差约 1e-16。

### 5.4 第一个积分 hop：即使从 source 出发也不一致

官方 `n_steps ≥ 1` 的第一跳就已经和 FD 不一致，此时 continuation 仍是单位阵，**不是多 hop 链式把 chart 乘错**。

| 事件 / target | meas | path | steps | loc0 rel | loc1 rel |
| --- | --- | --- | --- | --- | --- |
| 0 / t1 | 1 | −25.100 mm | 1 | 1.51 | 43.9 |
| 1 / t1 | 4 | 29.070 mm | 2 | 2.00 | 4.98 |
| 37 / t1 | 3 | 33.045 mm | 2 | 1.72 | 35.6 |
| 86 / t1 | 1 | −25.103 mm | 1 | 1.85 | 25.3 |

同一 hop 上 ACTS 终点 loc0 仍等于官方 predicted loc0（差值 0 或 ~1e-12）。两个映射在 `θ0` 的函数值一致，导数不一致。

后面的 0-step 立体 hop 会把这段积分解 Jacobian 继承下去，所以“全部 0-step 都和 FD 一致”不成立；只有 **source 出发的 0-step** 一致。

### 5.5 长磁 hop：函数值对齐，q/p 导数差几个数量级

`100043/0` t1 hop meas 6：path 3033.67 mm，93 steps，官方 − ACTS loc0 = 1.6e-11。  
ACTS `hop ∂loc0/∂(q/p) ≈ −3.45e-3`。正式 FD 过磁后 residual `q/p` 列约 614–1255。  
`100048/86` t1 hop meas 6：path 3026.02 mm，128 steps，loc0 差 −3.45e-10，ACTS `hop ∂loc0/∂(q/p) ≈ 2.84e-4`。

ACTS dummy-cov bound-to-surface Jacobian **没有**给出正式 `h_i` 在长磁 hop 上的 q/p 杠杆。

### 5.6 Dummy-cov 传播不是正式路径 — 37/86 chain 不完整

正式 Mode B 用无协方差、无边界 `SurfaceReached` + 支撑平面投影。诊断链用 dummy 1e-8 协方差 **传播到面** 才能填充 `transportJacobian`。

- `100043/0,1`：18/18 hop 都有 ACTS Jacobian，`chain_complete = true`，但与 FD 仍不一致。
- `100043/37`：长 hop 报 `Propagation reached the configured maximum number of steps`，后续 `Global to local transformation failed: position not on surface`。`chain_complete = false`。
- `100048/86`：部分 hop 同样 `position not on surface`。`chain_complete = false`。

37 的不完整 **不能**解释 0/1 的失败。正式路径本身在这些事件上仍 `nominal_ok = true`。

### 5.7 86 ACTS 列对 stepper 精度稳定 — 但不是 contract

86 的 `projection_composed` 列在 `1e-6` vs `1e-4`：

| target | loc1 rel | phi rel | q/p rel |
| --- | --- | --- | --- |
| 1 | 7.8e-5 | 4.7e-5 | 4.3e-5 |
| 2 | 6.5e-6 | 3.8e-7 | 4.1e-6 |
| 3 | 2.0e-5 | 7.2e-6 | 1.5e-5 |

`focus_acts_chain_stable = true`。**不**据此挑选 `1e-6` 做生产。稳定的是这条诊断 ACTS-chain，不是正式 `h_i` 的 FD，也不是和正式 FD 的一致性。

Continuation 全部是 `transform_free_to_bound`，无 `plane_chart_fallback`。`branch_switching = false`。

### 5.8 不允许的三种读法

1. **不是** `finite_difference_not_reliable_for_focus_transport`。该案需要 control 上 ACTS-chain 与 FD 一致。0/1/37 不满足。
2. **不是** `acts_transport_jacobian_not_numerically_converged`。86 上能写出的 ACTS 列不随预注册 tolerance 明显漂移。
3. **不是**物理不可微，也不是该改生产 `stepTolerance` 或减小 FD 步长。

## 6. 正式审计

Official run：`sbb14l_derivative_contract_20260908T180541Z_33bda85a`

| 文件 | SHA |
| --- | --- |
| `acts_fd_derivative_contract_decision.json` | `2388e8a8655eb1dc46e2b0207d7064f5689f9c69035f1f14b3b42bdd3ca7bf19` |
| `acts_chain_vs_fd_ladder.json` | `c9de5d93de2ab10f5a3f0dd58da8ecceef21bf9bf2723e416df5a8d2890ecb59` |
| `acts_chain_accuracy_stability.json` | `f80ef4aaa87d2497d54c9409866bfb9c8751075c0e37647c2853f31ecafd1be9` |
| `transport_projection_decomposition.json` | `8baea9c94f868fae97062a28df40e3f809cad6677e55e7f9e02cc69287a9df41` |
| config | `da58a62fc0378976bce0401977b3388015d62e2964daa716d47c2ff31322fc86` |
| helper | `8edc0d52378263b08ccefa8d87b419992e945507451d9238dc1173aa1828866c` |

`smoke_gate_passed = true` 只表示 0/1/37/86 的诊断材料齐全，**不**表示 Jacobian 通过。

## 7. 冻结

```
decision = analytic_chain_or_chart_contract_broken
jacobian_contract_established = false
b14m_reopen_authorized = false
restart_invariance_authorized = false
full_sample_authorized = false
b15_authorized = false
measurement_model_v2_authorized = false
do_not_replace_official_likelihood = true
do_not_select_best_step = true
do_not_select_best_tolerance = true
statistical_model_unchanged = true
```

主线仍然是：

```
WB118 mixed/inconclusive
  → WB119 analytic/ACTS-vs-FD derivative contract
  → derivative PASS          ← 本任务未达到
  → reopen B14M smoke
  → restart invariance
  → only then consider full-sample profile campaign
```

分类器留下的 `next_step` 是 `diagnose_jacobian_chain_chart_units_or_projection`。本 workbook 已经完成这一层检查：

- chart / 单位 / 残差符号 / 0-step 投影链：通过
- 破裂点：ACTS dummy-cov **bound-to-surface** `transportJacobian` 不是正式无边界支撑平面映射 `h_i` 的导数
- 下一步仍停在 transport / ACTS Jacobian API，**不**重开 B14M，**不**提交 1989

下一步允许做的，是继续给正式路径找一个与 0/1/37 FD 一致的解析/ACTS Jacobian 实现（例如从官方 unbounded+project 路径取导数，而不是从 dummy-cov 传到面的 `transportJacobian`）。不允许做的，是换一个更好看的 FD、改生产 `stepTolerance`、或把 ACTS 矩阵直接写进正式 likelihood。
