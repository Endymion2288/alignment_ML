# Workbook 126: Task B14ZA 生产与影子 Mean 传输契约闭合

日期：2026-09-09
状态：**完成 / FAIL** —— 以 GitHub `master` `55cf982302a3c62c57b74f368d5e3ba7723fd33a` 为 checkpoint，以 **WB125** 为最新冻结。在**不改变** WB114 measurement likelihood、不改生产 `stepTolerance=1e-4`、不改变正式 `h,h/2,h/4,h/8` FD ladder、不增加 FD rung、不缩小 track-state FD 步长、不放宽冻结 5% gate、不回调 WB124 DOPRI5、不按 Jacobian 符合度挑选或改写影子网格、不重用已失败的 WB125 20/10/5 mm 配方、不把正式 sequential likelihood 换成 direct-from-source、不加 prior / ridge、不删 `100043/37` 或 `100048/86`、不用 truth q/p、不调 Q / Cin、不修改 `FieldGradientDefaultExtension` / field-gradient variational implementation、**不评估也不读取任何 Jacobian**、不以修 5D Cin 为目标的前提下，只为 `100048/86` 的 **required first-unstable long magnet segments** 重建生产确定性 mean，并要求影子先闭合函数值。未重开 B14M，未做 restart invariance，未进 B15，未进 V4 C/D，未进 Measurement Model V2。未覆盖 WB109 / WB114–WB125 / `b14x_smoke` / `b14y_smoke` / `b14z_smoke` dumps。**未提交 1989 行 Condor**。禁止 analytic 自我认证。禁止把 target 2/3 已经收敛的 earlier hit 6 偷换成 required reference。禁止把真空 ODE 自动称为 production-map reference。

**最终判定：`FAIL` / Case `surface_energy_loss_semantics_mismatch`**

- `decision = surface_energy_loss_semantics_mismatch`
- `primary_case = surface_energy_loss_semantics_mismatch`
- `smoke_gate_passed = true`
- `control_0_pass = true`
- `control_1_pass = true`
- `control_37_pass = true`
- `mean_path_unchanged = true`
- `target_exclusion_holds = true`（3 行，0 leaked）
- `focus_independent_reference_established = false`
- `jacobian_contract_established = false`
- `b14m_reopen_authorized = false`
- `restart_invariance_authorized = false`
- `full_sample_authorized = false`
- `prior_introduced = false`
- `ridge_added = false`
- `statistical_model_unchanged = true`
- `five_percent_gate_unchanged = true`
- `derivative_not_evaluated = true`
- `jacobian_agreement_not_read = true`
- `grid_selected_from_mean_contract_only = true`
- `b15_authorized = false`
- `measurement_model_v2_authorized = false`
- `next_step = keep_shadow_mean_transport_contract`

这仍然不是 B14M PASS，也不是 Jacobian contract PASS，也不是 mean contract PASS。WB125 已经证明 common-grid 可以消掉自适应步进分支噪声，path length 也可以对上；剩下的问题不是终端 plane / path，而是**确定性状态更新语义**。本任务用生产 accepted-step 序列重放官方 RKN4 之后，位置和方向在第一个表面材料节点之前与生产对齐。按物理事件而不是 array index 对齐后，第一个明显分叉永远是 `surface_material`：`Δpos ≈ 0`、`Δdir ≈ 0`，裂开的是 `q/p`。生产在同一块已 path-corrected slab、同一个记录 `q/p` 上的 `ΔE`，落在 `computeEnergyLossMean` 与 `computeEnergyLossMode` **之间**。单位 internally 自洽。方向 normalization 有 `EigenStepper.ipp` 源码支持，但解释不了残差。预注册 mean-only `10/5/2.5/1.25 mm` 序列平台化，不是积分分辨率问题。

不允许把本跑判成 `shadow_mean_contract_established`。不允许因为 Shadow P 已经把场积分对齐、或 T3 loc0 已经到 4e-10 mm，就越过 q/p / 材料语义。不允许查看或引用任何 Jacobian 符合度。即使下一本 workbook 再生成 common-grid FD，也必须先把这个 mean 函数对齐。

## 起始状态

HEAD：`55cf982302a3c62c57b74f368d5e3ba7723fd33a`

WB125 FAIL：`shadow_mean_contract_not_established`  
Official run：`sbb14z_focus86_common_grid_shadow_20260909T130344Z_2aa503bd`  
Decision SHA：`15fbffbd06ef6747d3adecec9cdb6e64a0be9c9986bd08703e8e9c553f49d5c2`  
Config SHA：`547d727517659b2f5e8c20a14f282982fdfb71e0d4d34483b6ce640eba952dcc`  
Helper SHA：`3c1f1d41a79b2ff179173088d82057bcc31362822481f63c1e81610699763844`

```
decision = shadow_mean_contract_not_established
focus_independent_reference_established = false
jacobian_contract_established = false
b14m_reopen_authorized = false
control 0/1/37 PASS
mean_path_unchanged = true
next_step = keep_86_common_grid_shadow_mean_contract
```

WB125 名义残差（冻结，不回调门限）：

```
T1/hit6:  Δloc0=0.015000 mm, Δpos=0.015305, Δdir=1.31e-5, Δqop_rel=3.10e-3
T2/hit11: Δloc0=0.001310 mm, Δpos=0.003363, Δdir=9.21e-6,  Δqop_rel=4.54e-3
T3/hit11: Δloc0=0.000153 mm, Δpos=0.000167, Δdir=2.21e-6,  Δqop_rel=1.17e-4
```

WB123 已冻结且本任务未改：

```
FieldGradientDefaultExtension.hpp SHA
ca5e4f0ef1a7a09edd1c24099e7ce689c4f0511e2d4afab3a160ebd2d6ff03d8
control 0/1/37 PASS
official mean Δloc0 = 0
```

冻结 likelihood 不得改写：

```
theta = (loc0, loc1, phi, theta, q/p)
alpha = (loc0, theta)
nu    = (loc1, phi, q/p)
chi2(θ) = Σ r_i(θ)^T R_i^{-1} r_i(θ)
R_i = (0.08 mm)² / 12
```

WB103 冻结 denominator 保持：raw 2680 / ineligible 691 / contracted 1989 / official pairs 1974。

## 本任务做了什么

只跑 `100048/86`，required hops `1/6, 2/11, 3/11`。没有开 official supporting-plane Jacobian、没有开 field-gradient repair dump、没有开 WB124 DOPRI5、没有开 WB125 common-grid FD。`jacobian_validation` 在 mean-only 路径上被关掉。

1. 从生产 EigenStepper + Navigator + MaterialInteractor 导出只读 mean ledger（accepted field interval 与 surface material；不含 covariance / process noise）。
2. Shadow P 用**官方 RKN4 / Nyström**（`EigenStepper.ipp` 的位置/方向更新 + `GenericDefaultExtension` 的 `k_i` + accepted-step `normalize()`）在生产 accepted-step 序列上重放，材料节点按到达后更新 `q/p`。
3. Shadow D 使用预先注册、与 Jacobian 隔离的 mean-only 序列 `Δs, Δs/2, Δs/4, Δs/8 = 10, 5, 2.5, 1.25 mm`。
4. 按物理事件对齐，找第一个生产/影子分叉。
5. 逐表面审计 slab / pathCorrection / `computeEnergyLossMean` / `computeEnergyLossMode` / 生产 `q/p` before/after。
6. 机制 ablation：`field_only` / `field+material` / `field+material+normalize` / `full`。

预注册 mean-only 网格理由（写在执行前）：

```
10 mm = 一个 FASER 磁体网格尺度
RKN4 全局误差 O(h^4)
四次折半到 1.25 mm，进入单个 cell
残差消失 → 分辨率；残差平台 → 物理语义
derivative_not_evaluated = true
jacobian_agreement_not_read = true
grid_selected_from_mean_contract_only = true
```

## 正式产物

Official run：`sbb14za_shadow_mean_transport_20260909T151144Z_d24ca53b`

```
config SHA     297f8b47a0664bd130747403f36f72de508a3770d7018660cbfad110a02fd276
decision SHA   f4d65c51ab90c04d318e935d116b419f22a06dc1e6006f4f821d6b150ed2a426
helper SHA     20f40424d7154cbcaa4fa3efc0dc7b1add2ec80be1d4a6d05ea463db5fd000f8
mean header SHA 7ad33181d2d64f31d9114c382ac2c5c26f3767a0ba4229b186028baf37b22463
dump SHA       3930beaf31946fa493a50f14f82b62fc019dc9c2fbff2856cbe173d3922dead8
```

Dump：`outputs/leave_target_out_dump_v1/b14za_smoke/mc24_100048_00000_00049/ckf_leave_target_out_shadow_mean.jsonl`（3 行）

Artifacts：

```
production_mean_transition_ledger.json
shadow_mean_transition_ledger.json
production_shadow_first_divergence.json
surface_energy_loss_mean_contract.json
material_update_order_contract.json
qop_energy_loss_unit_contract.json
direction_normalization_contract.json
shadow_mean_convergence.json
shadow_mean_mechanism_ablation.json
required_segment_mean_failure_taxonomy.json
shadow_mean_transport_decision.json
inherited_stage.json
COMPLETE.json
```

## Shadow P 残差（生产 accepted-step RKN4 + 记录 slab 上的 computeEnergyLossMean）

```
T1 / hit6:
Δloc0 = -0.01690 mm
Δpath = -6.113 mm
Δpos  = 0.01723 mm
Δdir  = 1.52e-5
Δqop_rel = 3.10e-3

T2 / hit11:
Δloc0 = 0.00354 mm
Δpath = -6.113 mm
Δpos  = 0.00641 mm
Δdir  = 1.22e-5
Δqop_rel = 4.54e-3

T3 / hit11:
Δloc0 = 4.0e-10 mm
Δpath = -6.113 mm
Δpos  = 8.4e-10 mm
Δdir  = 2.8e-10
Δqop_rel = 1.17e-4
```

Path 的 −6.113 mm 在三个 hop 上相同，是影子重放对生产 `pathLength` 的系统偏移，不是终端 plane 问题。T3 的 loc0 / pos / dir 已经低于 mean gate；挡住 T3 的是 `q/p` 和这条 path 偏移。

## 第一个分叉

按 `field_propagation_interval` / `surface_material` 物理事件对齐，不是按数组下标。

```
T1/6:  first = surface_material #0 @ s=2.143 mm
       Δpos=0, Δdir~1e-16, Δqop_rel=1.95e-3

T2/11: first = surface_material #0 @ s=1120.108 mm
       Δpos~2e-13, Δdir~0, Δqop_rel=1.17e-4

T3/11: first = surface_material #0 @ s=1120.108 mm
       Δpos~2e-13, Δdir~0, Δqop_rel=1.17e-4
```

结论：官方 RKN4 在生产 accepted-step 上把**场积分**对齐了。WB125 剩下的 0.015 mm / 0.45% q/p **不是** classical RK4 vs RKN4 的主因，而是材料 mean 更新。

## 表面能量损失

`evaluateMaterialSlab` 源码确认：先按 start/target/intermediate 选 `PostUpdate` / `PreUpdate` / `FullUpdate`，再 `pathCorrection = surface->pathCorrection(...)`，再 `slab.scaleThickness(pathCorrection)`。记录下来的 `materialSlab` 已经包含 stage scale 和 incidence 修正。生产 ledger 上 `path_correction ≠ 1`，所以“影子只用名义厚度”不是本轮主因。

T1 第一块表面：

```
slab_thickness = 266.505 mm
path_correction = 1.00011
qop_before = -0.011841214
production qop_after = -0.011906198     (ΔE ≈ 0.461 GeV)
computeEnergyLossMean → qop = -0.011929405  (Eloss = 0.624 GeV)
computeEnergyLossMode → qop = -0.011888801  (Eloss = 0.338 GeV)
```

生产 `ΔE` 介于 Mean 与 Mode 之间。编译进 Acts Core 的 `evaluatePointwiseMaterialInteraction` **不是**在记录 slab + 记录 `q/p` 上直接调用 Mean 或 Mode。后续有的记录表面生产 `Δq/p = 0`，但 Mean 公式非零——影子若对每个记录节点都施加 Mean，会继续推偏后面的磁场区间。

单位合同成立：

```
ACTS 内部 q/p = 1/GeV
Eloss 输入输出 = GeV
厚度 = mm
muon mass = 0.105658 GeV
updateState 最低动量 = 10 MeV
```

不能从最后 `loc0` 反推 `q/p`。T1/T2 的 `Δqop_rel ~ 3e-3 – 4.5e-3` 比 loc0 更直接。

## 材料更新时刻

生产顺序（`MaterialInteractor.hpp`）：

```
propagate to surface
→ evaluateMaterialSlab
→ evaluatePointwiseMaterialInteraction
→ updateState(q/p only)
→ continue
```

起点 `PostUpdate`，终点 `PreUpdate`，中间 `FullUpdate`。体积材料只记录。场区间上 `q/p` 恒定。Shadow P 复制了“到达后再改动量”。第一个分叉处位置/方向仍重合，所以**不是** crossing 前/后更新把后续 `T×B` 立刻拉开的那种顺序错误；是 **Eloss 数值本身** 在第一个节点就和生产成本不一致。

## 方向 normalization

`EigenStepper.ipp` accepted-step：

```
pos += h T + h²/6 (k1+k2+k3)
dir += h/6 (k1 + 2(k2+k3) + k4)
dir.normalize()
```

Shadow P 只在每个 accepted field interval 之后做这一次源码支持的 normalize。Ablation 里 `field+material` 与 `field+material+normalize` 的 loc0 差在 1e-12 mm 量级。Normalization **不是** T1 0.015 mm 的原因。禁止为了 endpoint 更近再额外加 normalize。

## Mean-only 收敛

```
T1 loc0: 10 mm → -0.01648; 5 mm → -0.01641; 2.5 mm → -0.01654; 1.25 mm → -0.01656
T2 loc0: 10 mm →  0.00320; 5 mm →  0.00431; 2.5 mm →  0.00422; 1.25 mm →  0.00422
T3 loc0: 10 mm → -0.00015; 5 mm →  0.00017; 2.5 mm →  0.00016; 1.25 mm →  0.00017
q/p 在整个 D 序列上不变
```

没有朝生产 endpoint 收敛。`shadow_integrator_resolution_unresolved` 不成立。平台偏移指向物理语义，不是网格。

## 机制 ablation

```
T1 loc0:
  field_only                              +0.04386 mm   (回到 WB124 真空量级)
  field + material                        -0.01690 mm
  field + material + normalize            -0.01690 mm
  full                                    -0.01690 mm

T2 qop_rel:
  field_only                              1.48e-2
  field + material                        4.54e-3
  full                                    4.54e-3

T3 loc0:
  所有含材料的 variant                    ~4e-10 mm
  field_only                              ~1e-9 mm
  挡住 T3 的是 Δqop_rel = 1.17e-4 和 Δpath = 6.113 mm
```

材料 mean 更新解释了 T1 从 0.044 mm 降到 0.017 mm，也解释了 T2 q/p 从 1.5% 降到 0.45%。剩下的残差仍是**同一套能量损失语义**，不是场离散、也不是 normalize。

## T1 / T2 / T3 分类

允许三个 hop 机制不同。本轮证据是同一主因、不同可见症状：

```
T1 = surface energy-loss amount on the first thick slab
     + later over-applied Mean on zero-Δq/p recorded surfaces
     → loc0 0.017 mm, qop 0.31%

T2 = same q/p / material semantics on the first station-2 slab
     → loc0 0.0035 mm, qop 0.45%

T3 = 单次材料 q/p（1.17e-4，刚好压过 1e-4 门）
     + 共同的 +6.113 mm path 重放偏移
     loc0/pos/dir 已经闭合
```

三个 required hop 的 taxonomy 都是 `surface_energy_loss_semantics_mismatch`。

## 冻结旗标

```
control 0 PASS = true
control 1 PASS = true
control 37 PASS = true
mean_path_unchanged = true
focus_independent_reference_established = false
jacobian_contract_established = false
b14m_reopen_authorized = false
```

即使以后 mean contract PASS，后三个 false 也必须保持到**下一本** workbook 用已认证 mean shadow 重做 common-grid FD。

## 下一任务

继续留在 86 required hops。不要开 Jacobian。优先从 ACTS 32.0.2 **编译源**确认 `evaluatePointwiseMaterialInteraction` 实际写入的 `Eloss`（Mean / Mode / Bethe / 其它），以及哪些记录表面真正调用了 `updateState`。在那个公式对齐之前，任何 common-grid FD 比较都不得用于 PASS/FAIL。

```
先把函数本身算成同一个函数，再比较它们的导数。
```
