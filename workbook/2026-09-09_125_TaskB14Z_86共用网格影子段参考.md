# Workbook 125: Task B14Z Focus-86 Common-Grid Shadow Segment Reference Contract

日期：2026-09-09
状态：**完成 / FAIL** —— 以 GitHub `master` `55cf982302a3c62c57b74f368d5e3ba7723fd33a` 为 checkpoint，以 **WB124** 为最新冻结。在**不改变** WB114 measurement likelihood、不改生产 `stepTolerance=1e-4`、不改变正式 `h,h/2,h/4,h/8` FD ladder、不增加 FD rung、不缩小 track-state FD 步长、不放宽冻结 5% gate、不回调 WB124 DOPRI5 容差、不按 Jacobian 符合度挑选或改写影子网格、不把正式 sequential likelihood 换成 direct-from-source、不加 prior / ridge、不删 `100043/37` 或 `100048/86`、不用 truth q/p、不调 Q / Cin、不修改 `FieldGradientDefaultExtension` / field-gradient variational implementation、不以修 5D Cin 为目标的前提下，只为 `100048/86` 的 **required first-unstable long magnet segments** 建立与生产 mean **同一物理合同**的 common-grid shadow integrator。未重开 B14M，未做 restart invariance，未进 B15，未进 V4 C/D，未进 Measurement Model V2。未覆盖 WB109 / WB114–WB124 / `b14x_smoke` / `b14y_smoke` dumps。**未提交 1989 行 Condor**。禁止 analytic 自我认证。禁止把 target 2/3 已经收敛的 earlier hit 6 偷换成 required reference。禁止把真空 ODE 自动称为 production-map reference。禁止因为 target 3 production loc1 已经 3.53% PASS 就单独放行。

**最终判定：`FAIL` / Case `shadow_mean_contract_not_established`**

- `decision = shadow_mean_contract_not_established`
- `primary_case = shadow_mean_contract_not_established`
- `smoke_gate_passed = true`
- `control_0_pass = true`
- `control_1_pass = true`
- `control_37_pass = true`
- `mean_path_unchanged = true`
- `target_exclusion_holds = true`（12 行，0 leaked）
- `focus_independent_reference_established = false`
- `jacobian_contract_established = false`
- `b14m_reopen_authorized = false`
- `restart_invariance_authorized = false`
- `full_sample_authorized = false`
- `prior_introduced = false`
- `ridge_added = false`
- `statistical_model_unchanged = true`
- `five_percent_gate_unchanged = true`
- `b15_authorized = false`
- `measurement_model_v2_authorized = false`
- `next_step = keep_86_common_grid_shadow_mean_contract`

这仍然不是 B14M PASS，也不是 Jacobian contract PASS。WB123 已经证明缺失的 `∂dir/∂pos` 来自 `∂B/∂x`，修复后正式 mean path 完全不变，`100043/0,1,37` 全部通过冻结 5% derivative gate。WB124 用自适应 DOPRI5 真空 ODE 做独立 FD，失败原因是 `+δ/−δ` 各自 adaptive stepping 后使用不同 accepted-step sequence，而且真空 mean 与生产段最多还有约 0.044 mm 函数值差。本任务先审计生产 mean 的确定性内容，再在**同一冻结节点序列**上做独立 RK4 shadow。path length 与生产完全一致，common-grid `branch_identity_same = true`，WB124 的 accepted-step 分支噪声已被消掉。但名义影子 mean 没有通过预注册 mean-closure：T1/6 loc0 残差 0.015 mm，T2/11 loc0 残差 0.00131 mm，T3/11 的方向 / q/p 残差未过预注册 dir / qop 门。**函数值未闭合，因此不得认证导数。** 不得按 Jacobian 符合度回调网格或 mean 门限。不得再回去调 track FD 或 WB124 DOPRI5。不得重开 B14M。

不允许把本跑判成 `focus_common_grid_reference_established`。不允许因为 common-grid FD 自身看起来收敛、或 A 与 C 在数值上接近，就越过 mean contract。不允许因为 target 3 production loc1 3.53% PASS 就单独放行。

## 起始状态

HEAD：`55cf982302a3c62c57b74f368d5e3ba7723fd33a`

WB124 FAIL：`focus_segment_reference_not_established`  
Official run：`sbb14y_focus86_segment_reference_20260909T092655Z_18ab2ab0`  
Decision SHA：`f4ba49cffe187929c9eb79ae1e6e604a9852f29b8ef2ed7e8da3f7dcbbd471e7`  
Config SHA：`46ab8197bdbf40b6fa77c955ca5d5696f0fbbb7692213c2fa39edbe539162119`  
Helper SHA：`3558bc6bdedb326a5dc48bba2dc4d57833574b82eb66d1a14fa8106d45d3edf1`

```
decision = focus_segment_reference_not_established
focus_independent_reference_established = false
jacobian_contract_established = false
b14m_reopen_authorized = false
control_0/1/37 PASS
mean_path_unchanged = true
next_step = keep_86_independent_segment_reference
```

WB123 已冻结且本任务未改：

```
FieldGradientDefaultExtension.hpp SHA
ca5e4f0ef1a7a09edd1c24099e7ce689c4f0511e2d4afab3a160ebd2d6ff03d8
control 0/1/37 PASS
official mean Δloc0 = 0
missing_term_source_proven = true
```

冻结 likelihood 不得改写：

```
theta = (loc0, loc1, phi, theta, q/p)
alpha = (loc0, theta)
nu    = (loc1, phi, q/p)
chi2(θ) = Σ r_i(θ)^T R_i^{-1} r_i(θ)
R_i = (0.08 mm)² / 12
```

WB103 冻结 denominator 保持：raw 2680 / ineligible 691 / contracted 1989 / official pairs 1974。

冻结 FD ladder 保持：

```
h(loc0)=0.01 mm, h(loc1)=0.01 mm, h(phi)=1e-5, h(theta)=1e-5, h(q/p)=1e-6 /GeV
rungs: h, h/2, h/4, h/8
last-pair rel ≤ 0.05 and sign consistent
production stepTolerance = 1e-4
```

## 1. 本任务唯一问题

不是再修 field-gradient tangent，也不是再调 WB124 DOPRI5。唯一问题是：

> 对 `100048/86` 的 required first-unstable long magnet segments（target 1 hit 6，target 2 hit 11，target 3 hit 11），能否在**同一物理生产 mean contract**上，用冻结 common-grid 的独立 shadow integrator 消除 WB124 的 adaptive-step 分支噪声，并在 mean 先闭合的前提下建立独立 numerical derivative reference？

范围严格是 `100048/86` 的这三个 hop，加上对 `100043/0,1,37` 的 control 回放。不是 44。不是 1989。不重开 B14M。不重开 B14X 实现。不回调 B14Y DOPRI5。

## 2. Required hops：禁止偷换

```
FOCUS_FIRST_UNSTABLE = {1: 6, 2: 11, 3: 11}
```

C++ 对 `measurement_index ∈ {6,11}` 且 `n_steps > 10` 的 hop 写出 hop-start state / production FD / common-grid shadow。Python 只把 target-specific required index 当作契约对象。targets 2/3 的 hit 6 可以出现在 dump 里，但 `audit_required_segment` 必须读 hit 11。

## 3. 生产 mean 的物理内容（先于任何导数比较）

ACTS 32.0.2 生产 hop 使用 `EigenStepper` + `Navigator` + `MaterialInteractor`（`multipleScattering=true`, `energyLoss=true`）+ 无界 supporting-plane `SurfaceReached`。

`PointwiseMaterialInteraction::updateState`：

- 能量损失即使 `covTransport=false` 也会更新 mean `q/p`
- 多次散射只写协方差，不改方向 mean
- 体积物质被记录，但不应用到 ACTS mean
- process noise / covariance 不得进入影子 mean

本任务对每个 required hop 用正式 propagator 再跑一次 `recordInteractions=true` + `SteppingLogger`，得到确定性表面 crossing。结果：

| target / hit | n_surface | n_volume recorded | materialInX0 (ACTS accumulator) | 磁场 | 平面终止 | process noise in mean |
| --- | --- | --- | --- | --- | --- | --- |
| 1 / 6 | 5 | 0 | 35.374 | true | true | false |
| 2 / 11 | 4 | 0 | 51.267 | true | true | false |
| 3 / 11 | 1 | 0 | 1.351 | true | true | false |

独立 reference **必须**包含这些真实存在的确定性效应。不允许继续拿真空 ODE 自动称为 production-map reference。WB124 T1/6 真空 loc0 差 0.044 mm 已经证明这一点。`materialInX0` 是 ACTS `RecordedMaterial` 累加器，不单独当作认证量；认证看的是 endpoint free state / path / supporting-plane loc0。

## 4. 影子积分器：必须先预注册，才能看 86

`alignment/leave_target_out_dump/CommonGridShadowIntegrator.hpp`

```
method: classical_rk4_common_grid
ODE:    dr/ds = T;  dT/ds = (q/p) T × B(x);  d(q/p)/ds = 0   （场段）
eloss:  Acts::computeEnergyLossMean(slab, muon) 于冻结表面节点
B:      FASERMagneticFieldWrapper::getField
NOT:    EigenStepper / GenericDefaultExtension / FieldGradientDefaultExtension / WB124 DOPRI5
NOT:    official h_i(θ)
NOT:    repaired tangent 生成 FD
```

网格只由 **nominal 未扰动 hop** 生成。构造规则、最大步长和误差目标在看见任何 derivative agreement 之前写入 header / yaml，本跑未改：

```
coarse_max_step_mm   = 20
nominal_max_step_mm  = 10    # 一个 FASER 磁体格子尺度
fine_max_step_mm     = 5
mean_loc0_abs_mm     = 1e-3  # SCT σ=0.08/√12 的 1/23；比 WB124 真空 0.044 mm 紧 44 倍
mean_path_abs_mm     = 1e-2
mean_pos_abs_mm      = 1e-2
mean_dir_abs         = 1e-6
mean_qop_rel         = 1e-4
grid_deriv_rel_max   = 0.05
grid_mean_loc0_abs_mm = 1e-3
```

所有 `±h, ±h/2, ±h/4, ±h/8` 臂强制复用同一冻结 node sequence、同一 field/material crossing partition、同一 RK4 stages。禁止每个 perturbation 自己 adaptive stepping。

grid-refinement falsification 只作用于影子积分网格：coarse / nominal / fine 三套都预注册。不得根据 repaired analytic agreement 选择网格。

mean contract 先于一切导数：

```
若名义影子与生产 mean 在
  endpoint free state / path length / supporting-plane loc0
上不能同时通过预注册门限
→ shadow_mean_contract_not_established
→ 不能谈 derivative
```

## 5. 86 dump：common-grid，不跑 WB124 DOPRI5

Athena smoke 只重跑 `100048/86`。`100043/0,1,37` 复用冻结 WB123 dump。未覆盖 `b14x_smoke`、`b14y_smoke`、WB109、WB120。

```
outputs/leave_target_out_dump_v1/b14z_smoke/mc24_100048_00000_00049/ckf_leave_target_out_official_jacobian.jsonl
SHA 1f641557a465c0bde63e82bafb23954a228bde05278baa0d0f5a3c61937122c2
3 rows
```

冻结 dumps 未改：

```
b14x 100043  SHA b3a52180bb4f211b4352ee41f71762a8c159fe66e8291087ca14791f11f56582
b14y 100048  SHA 1888e9019fa5e4ce43b72bc6f6a51f9680251253ac199fb17bd01c763a5fb1f1
WB109        SHA 55e13b6a72de2e59129b0e4f71de0990c350bec7f6a4f0a4f8ae32e34215cfc8
FieldGradientDefaultExtension.hpp SHA ca5e4f0e…d6ff03d8
```

Flags：`--enable-official-supporting-plane-jacobian` + `--enable-field-gradient-variational-repair` + `--enable-focus86-common-grid-shadow`。**没有** `--enable-focus86-segment-reference`。场梯度扩展文件未改。

每个 required hop 都写出：生产 mean 物理效应、冻结 coarse/nominal/fine mesh、名义影子 mean 与 mean-closure、三套网格上的冻结四档 common-grid FD（每档 ±loc0、n_steps、path、`branch_identity_same`、`common_grid_reused`）。

## 6. Mean contract（先于导数；本跑在这里失败）

| target | hit | official n_steps / path mm | shadow n_field / n_mat / path mm | official loc0 | shadow loc0 | Δloc0 | Δpath | Δpos | Δdir | Δqop rel | closed? |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 6 | 128 / 3026.02306 | 306 / 5 / 3026.02306 | 24.722918 | 24.737919 | 0.015000 | 0 | 0.015305 | 1.31e-5 | 3.10e-3 | **no** |
| 2 | 11 | 80 / 2316.36152 | 235 / 4 / 2316.36152 | 19.786014 | 19.784705 | 0.001310 | 0 | 0.003363 | 9.21e-6 | 4.54e-3 | **no** |
| 3 | 11 | 42 / 1126.22103 | 114 / 1 / 1126.22103 | 24.722712 | 24.722864 | 0.000153 | 0 | 0.000167 | 2.21e-6 | 1.17e-4 | **no** |

预注册门限：loc0 ≤ 1e-3 mm，path ≤ 1e-2 mm，pos ≤ 1e-2 mm，dir ≤ 1e-6，qop_rel ≤ 1e-4。

相对 WB124 真空 ODE：T1/6 loc0 差从 0.044 mm 降到 0.015 mm，T2/11 从 0.011 mm 降到 0.00131 mm，T3/11 仍约 0.00015 mm。包含表面能量损失后函数值更接近生产 mean，但**没有**通过预注册门。T1/6 同时破 loc0 / pos / dir / qop。T2/11 破 loc0 / dir / qop，且 nominal−fine loc0 = 0.00289 mm，grid mean 不稳定。T3/11 loc0/path/pos 过门，但 dir 2.21e-6 和 qop_rel 1.17e-4 略超门限。

Hop-start bound（ACTS 内部：loc0/loc1 mm，phi/theta rad，q/p 1/GeV）与 WB124 相同：

```
T1 hit 6:  (−26.2234, −10.4752, −0.57316, 0.015072, −0.011841) at z = −1822.25 mm
T2 hit 11: ( 25.1238,  12.5724, −0.38547, 0.013722, −0.011948) at z =    77.355 mm
T3 hit 11: 同 T2 hit 11 start（同一 sequential 前段；终点不同）
```

`n·dir` 约 −1.0，不是近平行终端事件。体积物质 crossings = 0。正式 repair mean `official_minus_repair_mean_loc0 = 0`。影子 loc0 差只是诊断，**不**替换正式 `predicted_loc0`。

因为三个 required hop 的 mean contract 都未闭合：

```
decision = shadow_mean_contract_not_established
focus_independent_reference_established = false
```

下面记录的 A/B/C 数字只是 dump 里已经写出的诊断，**不是认证，也不能用来改判。**

## 7. 诊断记录（不得用于认证）

符号：A = production repaired `hop_dloc0_d_start`；B = production EigenStepper 冻结四档 hop-start FD；C = independent common-grid shadow mean FD（nominal mesh）。相对误差相对第二自变量。门限 last-pair rel ≤ 0.05 且同号。所有 C 臂 `branch_identity_same = true`，`plus_n_steps = minus_n_steps`。

### Target 1 / hit 6

| 参数 | A repaired | B production FD (h … h/8) | B last-pair | C nom (h … h/8) | C last-pair | C vs fine | A vs C | 独立建立？ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| loc1 | −0.06991 | −0.0785, −0.1175, −0.0248, −0.0563 | 56.0% | −0.06980, −0.06961, −0.06960, −0.06961 | 0.013% | 0.36% | 0.43% | no（mean 未闭合） |
| phi | 37.045 | 15.1, 83.3, 181.7, −327.0 | sign change | 37.045, 37.049, 37.041, 37.045 | 0.010% | 0.023% | 0.0019% | no |
| q/p | −610.154 | −783, −1583, −1473, +2318 | sign change | −618.57, −618.57, −618.61, −618.57 | 0.006% | 0.002% | 1.36% | no |

mesh：coarse 160 / nominal 311 / fine 614 nodes；5 个表面物质节点。grid mean loc0 stable（nom−fine = 6.0e-5 mm）。

### Target 2 / hit 11（禁止用 hit 6 替代）

| 参数 | A repaired | B production FD (h … h/8) | B last-pair | C nom (h … h/8) | C last-pair | C vs fine | A vs C | 独立建立？ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| loc1 | −0.33262 | −0.355, −0.320, −0.543, −0.088 | 515% | −0.33626, −0.33628, −0.33631, −0.33630 | 0.002% | 0.61% | 1.10% | no（mean 未闭合） |
| phi | 26.993 | 54.4, −92.8, 114.7, 640.4 | sign change | 26.982, 26.979, 26.980, 26.980 | 0.002% | 0.003% | 0.046% | no |
| q/p | −215.439 | +974, +2173, −2080, −9802 | sign change | −213.43, −213.45, −213.44, −213.50 | 0.028% | 0.13% | 0.91% | no |

mesh：coarse 123 / nominal 239 / fine 471 nodes；4 个表面物质节点。grid mean loc0 **unstable**（nom−fine = 0.00289 mm > 1e-3）。

Target 2 earlier hit 6（**不是** required）：生产 loc1 FD 仍收敛，本审计未把它当作 reference。

### Target 3 / hit 11（禁止用 hit 6 替代；禁止用 production loc1 3.53% 单独放行）

| 参数 | A repaired | B production FD (h … h/8) | B last-pair | C nom (h … h/8) | C last-pair | C vs fine | A vs C | 独立建立？ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| loc1 | −0.06523 | −0.07409, −0.07669, −0.06533, −0.06772 | **3.53% PASS** | −0.06612, −0.06612, −0.06612, −0.06612 | 0.004% | 1.00% | 1.34% | no（mean 未闭合） |
| phi | 14.089 | 15.07, 0.27, 18.39, 66.95 | 72.5% | 14.086, 14.085, 14.086, 14.086 | 0.001% | 0.005% | 0.020% | no |
| q/p | −101.620 | +92, +250, −472, −958 | sign change | −101.62, −101.63, −101.63, −101.65 | 0.021% | 0.035% | 0.028% | no |

mesh：coarse 59 / nominal 115 / fine 228 nodes；1 个表面物质节点。grid mean loc0 stable（nom−fine = 3.2e-4 mm）。T3 production loc1 last-pair 3.53% 仍然只是生产 B，不是独立 C，也不能单独放行。

## 8. 为什么即使 C 看起来内部稳定也不能认证

WB124 的核心数值风险——每个 perturbation 自己 adaptive stepping——在本跑已被消除：三套网格、全部参数、全部四档的 `branch_identity_same = true`。这是本任务完成的那一半。

另一半是 **同物理 mean contract**。预注册门限在看 Jacobian 之前就钉死。三个 required hop 的名义影子都没有同时通过 loc0 / pos / dir / qop 门。合同写明：函数值不能闭合，则直接判 `shadow_mean_contract_not_established`，不能谈 derivative。

因此不得把上面 A≈C 的百分数读成 `focus_common_grid_reference_established`。也不得因此去放宽 1e-3 mm loc0 门、放宽 1e-6 dir 门、或把 nominal 网格改成 fine。那些数是预注册的，本跑未改，下一任务如果要改配方，必须在再次看见 Jacobian 之前重新预注册，而不是按本表回调。

剩余 mean 残差的诊断（不是授权去调参）：

```
path 已精确闭合（网格用生产 pathAbs）
表面能量损失已进入影子（T1/6 真空 0.044 → 0.015 mm）
剩余 gap 在 dir / qop / 亚毫米 loc0
可能来自：表面 slab 的入射修正与影子节点放置、
          RK4 10 mm 与 EigenStepper adaptive 的局部截断、
          或能量损失施加时刻与生产 actor 不完全同序
不得据此回调 track FD，也不得回调 WB124 DOPRI5
```

T2/11 的 grid mean loc0 在 10 mm → 5 mm 下仍有 0.00289 mm 移动，说明即便只看函数值，影子网格分辨率对这条 hop 也还没在预注册门内稳住。

## 9. Control 0/1/37 继续 PASS

复用 WB123 `b14x_smoke` 100043 dump，12 行 invariance 全部 `mean_path_unchanged`。`audit_control_columns` 回放：

```
control_0_pass = true
control_1_pass = true
control_37_pass = true
```

正式 86 repair mean Δloc0 = 0。target leakage = 0。统计模型未改。5% gate 未改。`FieldGradientDefaultExtension.hpp` SHA 未改。

## 10. 不允许的读法

1. **不是** common-grid 独立 reference 已建立。mean contract 未闭合。
2. **不是**可以因为 C 四档自身收敛、或 A≈C ≪ 5%，就越过 mean contract 去认证 Jacobian。
3. **不是**可以把 T2/T3 hit 6 的收敛 production FD 当成 required reference。
4. **不是**可以把 T3 hit 11 的 **production** loc1 FD 3.53% PASS 当成独立参考。
5. **不是** `focus_common_grid_reference_established`，也不是 `repaired_variational_focus_inconsistent`。独立参考根本还没建立。
6. **不是**可以把本跑 C 的内部收敛读成可以放宽 mean 门限或改网格。
7. **不是**可以放宽 5%、增加 rung、缩小 track FD、改生产 `stepTolerance`、回调 WB124 DOPRI5。
8. **不是**已经可以重开 B14M 或提交 1989。
9. **不是**正式 mean / 正式 likelihood 被改写。影子 loc0 差只是诊断，不替换 `h_i`。
10. **不是** material / Q / Cin / prior / ridge / truth q/p 主线。
11. **不是**可以把影子积分器写进正式 likelihood。
12. **不是**真空 ODE 已经是 production-map reference。

## 11. 正式审计

Official run：`sbb14z_focus86_common_grid_shadow_20260909T130344Z_2aa503bd`

Login tests：`tests/test_focus86_common_grid_shadow.py`（5 passed）。

| 文件 | SHA |
| --- | --- |
| `production_mean_physical_effects.json` | `1427510a6e014c0c8a1cb85ca6a8afdcb8d358c978c495318b8b3e859c9352d5` |
| `frozen_common_grid_meshes.json` | `8503d2e3c5d6722917b0847c64c2abac56ce7a475cb54b2886aaf2ea0ac7284a` |
| `shadow_mean_contract.json` | `df8c23eb021dcb9c8dcafd9287d947dbd6e5ecaaa6bf071d05e0884eaddcbb60` |
| `common_grid_segment_fd.json` | `6dc558fabf76f7ddbc11cb149196aea1ddb31cfd590eca80c64d30b2ebbc23e3` |
| `shadow_grid_refinement.json` | `053a2b509ed96904fb144e064055d07ec675af2c24f64bb78e71410fd8825be1` |
| `triple_comparison_repaired_productionfd_commongrid.json` | `82b102123ba232ed13e828951c7fd92bfda52b296b4d933235426eb7e18e5bfc` |
| `control_0_1_37_gate_replay.json` | `2e7659cda642dc4577a1734d1afe84c857cd0925c3e2f7da5f06e2253ddf2b9a` |
| `focus86_common_grid_shadow_decision.json` | `15fbffbd06ef6747d3adecec9cdb6e64a0be9c9986bd08703e8e9c553f49d5c2` |
| `inherited_stage.json` | `8f7c7eb44d23455f09befc6b27cdc833d96d16fac24fb4ea00f9e622a9066034` |
| `COMPLETE.json` | `691c1785f0f36e92b510748bf522f6c49e4fc62265328500b98eac8dcd6d0396` |
| config | `547d727517659b2f5e8c20a14f282982fdfb71e0d4d34483b6ce640eba952dcc` |
| helper `CkfLeaveTargetOutDumpAlg.cxx` | `3c1f1d41a79b2ff179173088d82057bcc31362822481f63c1e81610699763844` |
| `CommonGridShadowIntegrator.hpp` | `be1e086040adc4e2c1b45732ceb21a0b49422731843655a322ffe3d049d8dce5` |
| `FieldGradientDefaultExtension.hpp` | `ca5e4f0ef1a7a09edd1c24099e7ce689c4f0511e2d4afab3a160ebd2d6ff03d8` |
| b14z 86 dump | `1f641557a465c0bde63e82bafb23954a228bde05278baa0d0f5a3c61937122c2` |

`smoke_gate_passed = true` 只表示 WB123/WB124/WB120 hash、12 行 dump、required hops 是 6/11/11、exclusion 与冻结 gate 材料齐全，**不**表示独立 reference 或 Jacobian contract 通过。

重新认证 derivative 的条件仍未满足：

```
control 0 PASS                          ← true
control 1 PASS                          ← true
control 37 PASS                         ← true
mean path unchanged                     ← true
86 common-grid mean contract            ← false
86 common-grid C established + A≈C
5% gate unchanged                       ← true
```

因此：

```
jacobian_contract_established = false
b14m_reopen_authorized = false
```

## 12. 冻结

```
decision = shadow_mean_contract_not_established
jacobian_contract_established = false
b14m_reopen_authorized = false
restart_invariance_authorized = false
full_sample_authorized = false
b15_authorized = false
measurement_model_v2_authorized = false
focus_independent_reference_established = false
control_0_pass = true
control_1_pass = true
control_37_pass = true
mean_path_unchanged = true
five_percent_gate_unchanged = true
do_not_relax_five_percent_gate = true
do_not_change_official_fd_ladder = true
do_not_add_fd_rung = true
do_not_shrink_fd_step = true
do_not_change_production_step_tolerance = true
do_not_change_official_mean_path = true
do_not_tune_wb124_dopri5 = true
do_not_tune_grid_from_jacobian = true
do_not_change_field_gradient_variational_implementation = true
do_not_self_certify_analytic = true
do_not_substitute_earlier_long_hop = true
do_not_call_vacuum_a_production_map_reference = true
do_not_replace_official_likelihood = true
statistical_model_unchanged = true
small_physical_effect_does_not_pass_contract = true
next_step = keep_86_common_grid_shadow_mean_contract
```

主线：

```
WB123:
field-gradient tangent repaired          ← 冻结；本任务未改
controls 0/1/37 PASS                     ← 本任务回放仍 PASS
official mean unchanged                  ← 本任务仍 Δloc0 = 0
        ↓
WB124:
adaptive independent FD failed
accepted-step branch noise
vacuum mean gap ≤ 0.044 mm
        ↓
WB125:
common-grid RK4 shadow
same physical mean (field + surface Eloss + plane)
branch_identity_same = true              ← adaptive-step 噪声已消除
path 与生产精确闭合
mean contract NOT closed                 ← loc0/dir/qop 未过预注册门
        ↓
Case shadow_mean_contract_not_established
        ↓
停住：继续 86 common-grid 的 mean contract
不得按 Jacobian 回调网格 / mean 门限
不得回调 WB124 DOPRI5
不得再调 track FD
不得 analytic 自我认证
不得重开 B14M
不得提交 1989
```

下一步仍然只处理 86 的独立 numerical reference，而且必须先让 **同物理 mean contract** 闭合。新的影子配方如果出现，其网格规则、最大步长和 mean-closure 门限必须在再次看见 86 Jacobian 符合度之前预注册；不能在本跑 10/20/5 mm 网格或 1e-3 mm loc0 门上按结果调到“通过”。不增加 track FD rung，不缩小 track FD，不改生产 `stepTolerance`，不放宽 5%，不回调 WB124 DOPRI5，不用 repaired tangent 自我认证，不把 T2/T3 hit 6 当成 required hop，不把 T3 production loc1 3.53% 单独放行。不重开 B14M，不提交 1989。
