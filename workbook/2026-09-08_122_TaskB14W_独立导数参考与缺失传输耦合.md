# Workbook 122: Task B14W Independent Derivative Reference and Missing Transport-Coupling Audit

日期：2026-09-08
状态：**完成 / FAIL** —— 以 GitHub `master` `55cf982302a3c62c57b74f368d5e3ba7723fd33a` 为 checkpoint，以 **WB121** 为最新冻结。在**不改变** WB114 measurement likelihood、不重调 Gauss–Newton、不改生产 `stepTolerance=1e-4`、不改变正式 `h,h/2,h/4,h/8` FD ladder、不增加 FD rung、不缩小 FD 步长、不放宽冻结 5% gate、不按结果挑最好 FD 步长或积分精度、不把正式 sequential likelihood 换成 direct-from-source、不用任何 ACTS Jacobian 替换正式 likelihood、不加 prior / ridge、不删 `100043/37` 或 `100048/86`、不用 truth q/p、不以修 5D Cin 为目标、**不改正式 `rk_free` Jacobian 实现**的前提下，只对 `100043/1` 与 `100048/86` 分开处理 WB121 冻结的两类残余。未重开 B14M，未做 restart invariance，未进 B15，未进 V4 C/D，未进 Measurement Model V2。未覆盖 WB109 / WB114 / WB115 / WB116 / WB117 / WB118 / WB119 / WB120 / WB121 dumps。未重跑 Athena。**未提交 1989 行 Condor**。禁止把 dummy-cov bounded `transportJacobian` 当作正式导数。

**最终判定：`FAIL`**

- `decision = missing_deterministic_transport_coupling_identified`
- `primary_case = missing_deterministic_transport_coupling_identified`
- `smoke_gate_passed = true`
- `jacobian_contract_established = false`
- `b14m_reopen_authorized = false`
- `restart_invariance_authorized = false`
- `full_sample_authorized = false`
- `control1_station0_loc1_agrees = true`
- `rk_magnet_hop_pos_to_dir_is_zero = true`
- `material_reset_is_highest_priority_mechanism = false`
- `focus_independent_reference_established = false`
- `target_exclusion_holds = true`（12 行，0 leaked）
- `prior_introduced = false`
- `ridge_added = false`
- `statistical_model_unchanged = true`
- `five_percent_gate_unchanged = true`
- `do_not_relax_five_percent_gate = true`
- `do_not_change_official_fd_ladder = true`
- `do_not_add_fd_rung = true`
- `do_not_shrink_fd_step = true`
- `do_not_change_jacobian_implementation = true`
- `do_not_use_dummy_cov_bounded_transportJacobian = true`
- `small_physical_effect_does_not_pass_contract = true`
- `b15_authorized = false`
- `measurement_model_v2_authorized = false`
- `next_step = repair_missing_pos_to_dir_variational_coupling_then_recontract`

这仍然不是 B14M PASS。WB121 已经把两类残余分开：control 1 是真实 derivative implementation residual，86 是 FD reference 不够精确。本任务找到了 control 1 后续 hits 多出来的 loc1 耦合的数学来源：磁 hop 的 `rk_free` free-transport Jacobian 有 `∂dir/∂pos ≡ 0`，而正式 mean FD 看到了场图的空间依赖。86 的冻结四档仍然振荡 / 变号，Richardson 必须标 `not_applicable`，独立 reference **没有**建立。5% gate 未改。未重开 B14M。未提交 1989。

不允许把本跑判成 derivative contract PASS：control 1 仍未过冻结 5%，86 仍无独立 reference。不允许因为残差效应只有 strip sigma 的 0.2% 就忽略 mismatch。不允许把 material reset 说成最高优先级机制。不允许继续缩 `h → h/16 → h/32`。不允许仅因为 analytic 稳定就用 ACTS variational 自我认证。找到缺失耦合只授权**下一步**修 derivative implementation，然后重新做 control+86 contract；本任务内部不改 Jacobian。

## 起始状态

HEAD：`55cf982302a3c62c57b74f368d5e3ba7723fd33a`

WB121 FAIL：`mixed_or_inconclusive`  
Official run：`sbb14v_official_residual_20260908T194821Z_c5b463f5`  
Decision SHA：`60d0ced8355b15c98f9758974299b0251b7500d8514fcffd966373bd5b780b44`  
Config SHA：`3ac25711142694bc3e4eaede110c5653fe6abc2d2ca2ac0552ef1e2d5238a287`  
Helper SHA：`bea305738d479f335fd64b1f7a92e0d9daf66a395873d17dda18bfa2e19927c3`

```
decision = mixed_or_inconclusive
jacobian_contract_established = false
b14m_reopen_authorized = false
five_percent_gate_unchanged = true
control_five_percent_pass = false
focus_fd_converged = false
active_residual_kinds =
  official_path_derivative_still_inconsistent
  fd_reference_not_precise_enough_for_derivative_certification
next_step = keep_residual_diagnosis_without_shrinking_fd
```

WB121 已冻结：

```
A. 100043/1 loc1
   FD ladder 已经收敛
   analytic vs FD = 5.6–6.2% mismatch
   |Ja-JFD| ≈ 90 × FD self-envelope
   => 真实 derivative implementation residual

B. 100048/86
   analytic 与 FD 已同量级
   FD h,h/2,h/4,h/8 自身不收敛
   analytic-vs-FD mismatch 与 FD rung-to-rung variation 同量级
   => FD reference 不够精确，不能继续缩步长认证
```

冻结 likelihood 不得改写：

```
theta = (loc0, loc1, phi, theta, q/p)
alpha = (loc0, theta)
nu    = (loc1, phi, q/p)
chi2(θ) = Σ r_i(θ)^T R_i^{-1} r_i(θ)
R_i = (0.08 mm)² / 12
```

WB103 冻结 denominator 保持：raw 2680 / ineligible 691 / contracted 1989 / official pairs 1974。

冻结 FD ladder 保持：

```
h(loc0)=0.01 mm, h(loc1)=0.01 mm, h(phi)=1e-5, h(theta)=1e-5, h(q/p)=1e-6 /GeV
rungs: h, h/2, h/4, h/8
last-pair rel ≤ 0.05 and sign consistent
```

Helper SHA 未改：`bea305738d479f335fd64b1f7a92e0d9daf66a395873d17dda18bfa2e19927c3`

## 1. 本任务唯一问题

把 A 和 B 分开，不换 Jacobian，不缩 FD：

1. `100043/1` 后续 measurement 上 FD 多出的 0.001–0.002 loc1 coupling，为什么没有进入 `rk_free` chain？从哪一个 hop / 哪一段 map 开始出现？
2. 该 extra coupling 是不是 material / curvilinear reset 漏掉的 deterministic Jacobian？只研究 mean-state deterministic derivative，不把 covariance process noise 混进来。
3. 关键 hop 的 mismatch 是某一个 propagation segment 内部 derivative 错，还是 segment Jacobian composition / reparameterization 漏项？
4. 86 能否在不增加新 rung、不挑最好 rung、不改变生产 FD 的前提下，从已有 `h,h/2,h/4,h/8` 建立独立 derivative reference？

范围严格是 `100043/1` 与 `100048/86`。不是 44。不是 1989。

## 2. 实现

Config：`configs/independent_derivative_reference_v1.yaml`  
SHA：`b68b826e7098d51338ce42e8613d250a09600bd9cfea4c852ec322b34a33d9c7`

输入是冻结 WB120 smoke，哈希必须匹配：

| dump | bytes | SHA |
| --- | --- | --- |
| `b14u_smoke/mc24_100043_00400_00499/ckf_leave_target_out_official_jacobian.jsonl` | 2241304 | `1c2daf7fda4e585d62624d1705934c3d621a69d560881f3d417d8a96bb1343ab` |
| `b14u_smoke/mc24_100048_00000_00049/ckf_leave_target_out_official_jacobian.jsonl` | 732933 | `831a2a3fc9ff6e0aa519895d642a5de576c42a3a947700fab48df58bb798a19d` |

Dataset：`datasets/independent_derivative_reference.py`  
只读已转储的 hop 矩阵与冻结 FD 列。对每个 hop 记录 loc1 在

```
source bound → free
RK variational transport
last-reset jacTransport
supporting-plane intersection
free→bound continuation
next-hop source
```

上的列贡献。不增加新的 C++ Jacobian，也不重跑 Athena。segment-level FD 使用冻结全链 FD + hop 级 RK 作为 proxy；**没有**新的 hop-start Athena FD。Richardson / 局部奇对称多项式只作诊断，不替换正式 FD。

测试：`tests/test_independent_derivative_reference.py` 8 passed（login，无 Athena）。分类器只允许六类判决；禁止 prior / ridge / 换 likelihood / 挑最好步长 / 增加 FD rung / 放宽 5% / 换 Jacobian / dummy-cov bounded Jacobian / 重开 B14M / 提交 1989。振荡序列的 Richardson 必须标 `not_applicable`。

## 3. Smoke（login only：复用 WB120 dumps）

未覆盖 WB109 official dump（仍 1951622 bytes）  
未覆盖 WB120 smoke（上表哈希不变）  
未覆盖 WB117 / WB118 / WB119 / WB121 dumps  

12 行正式 Mode B 仍是 `100043/0,1,37` 与 `100048/86` × target 1/2/3。本任务耦合审计只跑 `100043/1`；独立 reference / 逐 hit 分解只跑 `100048/86`。

## 4. Control 1：缺失的 loc1 transport coupling

三个 target 全部 `coupling_identified = true`。station 0 的 6 个 hits：FD = analytic stereo `±0.02`，ΔJ ~ 1e-13。

第一个 `|ΔJ| > 5e-5` **永远是 hit 6**，也就是第一条长磁 hop：

| target | hit | station | steps | path (mm) | resets | surfaces | J_FD | J_rk | ΔJ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 6 | 2 | 100 | 3034 | 5 | 77 | 0.01916 | 0.02000 | −0.00084 |
| 2 | 6 | 1 | 75 | 1844 | 2 | 64 | 0.01973 | 0.02000 | −0.00027 |
| 3 | 6 | 1 | 75 | 1844 | 2 | 64 | 0.01973 | 0.02000 | −0.00027 |

target 1 后续 hits 的 ΔJ 增长到 station 3 的 −0.0019…−0.0021。每个 target 有 6 个 **0-step** hits 仍然带着 ΔJ：这些 hop 没有新的 RK，也没有新的 material，extra coupling 是继承的。

hit 6 的 loc1 chain（三个 target 结构相同）：

```
source bound → free loc1 col = (0.9998, -0.0200, 0, 0, 0, 0, 0, 0)
RK product     ∂dir/∂pos = 0
last-reset     ∂dir/∂pos = 0
RK · (bound→free loc1) = 同一向量，方向块仍为 0
hop dloc0/d start loc1 = -0.03999   (hop-start 立体投影)
chained dloc0/d source loc1 = -0.02000
continuation loc1 = (-0.040, 0.9992, 0, 0, 0, 0)
```

continuation 的 φ / θ / q/p 对 source loc1 的导数是 0。因此 `rk_free` 链在磁 hop 之后仍然只传输立体投影，不会把横向位置耦合进方向。正式 mean FD 在穿过场图时看到了空间依赖，所以后续 hits 多出 0.001–0.002。

**唯一问题的答案：** FD 中后续 measurement 出现的额外 loc1 sensitivity 没有进入 `rk_free` chain，是因为磁 hop 的 free-transport variational Jacobian 把 `∂dir/∂pos` 写成了恒零。这是缺失的 deterministic transport coupling，不是 composition 算错。

## 5. Material reset 审计

磁 hop 上确实有 collector reset（target 1：5 次；target 2/3：2 次），几何面序列也很长。但：

- 终点 SCT measurement 面 `has_surface_material = false`
- **完整** RK 积的 `∂dir/∂pos` 已经是 0，不只是 last-reset 之后
- 正式 mean propagation 不依赖 covariance；本任务只研究 mean-state deterministic derivative
- 未把 covariance transport 或 process noise 混进官方导数

`material_reset_is_highest_priority_mechanism = false`。reset 与 extra coupling 同时出现，但漏项已经在 RK variational 积本身，不是 reset 把一项已有的 `∂dir/∂pos` 清掉。下一步应修 / 暴露场梯度或 `∂dir/∂pos` 的 variational 实现，而不是先改 material Q。

## 6. Segment-level derivative closure

没有新的 hop-start Athena FD，也没有 step hunting。localization 来自冻结语义：

| 观察 | 结论 |
| --- | --- |
| station 0 全链 FD = `rk_free` | 这些短段内部闭合 |
| 第一个非零 ΔJ 在长磁 hop | leak 从该 hop 开始 |
| 该 hop 的 RK `∂dir/∂pos = 0`，continuation 角向 loc1 = 0 | variational 漏项在磁 hop |
| 后续 0-step hits 仍有 ΔJ | 不是这些短段内部算错，而是继承 |
| WB121 dumped-matrix composition 相对误差 = 0 | 不是 composition / chart 乘漏 |

因此：

- **不是 B** `segment_variational_derivative_inconsistent`：没有证据表明某一个短段的 RK 与该段自身 map 的 FD 不一致。
- **不是 C** `derivative_composition_or_reparameterization_incomplete`：已转储矩阵的 composition 是闭合的；0-step 继承把泄漏钉在磁 hop variational 漏项上。
- **是 A**：磁 hop 的 deterministic variational map 缺少 `∂dir/∂pos`。

`segment_fd_kind = frozen_full_chain_fd_plus_hop_rk_proxy`。`new_hop_start_segment_fd_computed = false`。

## 7. 86：独立 derivative reference

三条路线都没有把 `independent_reference_established` 设为 true。

### Route 1 — Richardson

只用已冻结的 `h, h/2, h/4, h/8`。序列振荡或变号则必须 `not_applicable`，不能强算。三个 target × loc1/phi/theta/q/p 的列 norm 序列全部 `not_applicable`。

target 1 hit 6 的有符号序列（不是列 norm）同样失败：

| 参数 | h | h/2 | h/4 | h/8 | 状态 |
| --- | --- | --- | --- | --- | --- |
| loc1 | 0.046 | 0.152 | 0.104 | 0.034 | 非单调 |
| phi | −50 | −18 | −57 | −282 | 爆炸 |
| theta | 1770 | 1797 | 1811 | 1532 | 非渐近 |
| q/p | 783 | 1220 | 1206 | −2318 | `h/8` 变号 |

### Route 2 — 局部奇对称多项式

对已有四档拟合 `J(h) = J0 + a h² + b h⁴`。设计矩阵条件数约 23，数值上能出斜率，但这只是诊断：序列本身不是渐近截断模型。`do_not_replace_official_fd = true`。不能把拟合斜率当成新的正式导数，也不能挑最好 rung。

### Route 3 — ACTS variational self-reference

禁止。control 1 已经证明当前完整 deterministic variational chain 漏了 `∂dir/∂pos`。不能仅因为 analytic 稳定就自我认证。

86 的独立 reference 状态对应 E：`independent_reference_not_established`。主判决仍是 A，因为 control 1 的缺失耦合已经定位；86 记在 `focus86_independent_derivative_reference.json`。

## 8. 86 逐 hit，不只看整列 norm

station 0 对 loc1 / phi / theta 稳定，与 analytic 立体 / 有限角度一致。不稳定不是“所有 hit 从一开始一起漂”，而是**第一条剩下的长 hop 之后同向放大**。

| target | first unstable | station | steps | loc1 rung spread | 后续模式 |
| --- | --- | --- | --- | --- | --- |
| 1 | hit 6 | 2 | 128 | 0.118 | 11 个下游 hits 不稳定；station 3 最大 |
| 2 | hit 11 | 3 | 80 | 0.772 | 5 个下游 hits；station 3 主导 |
| 3 | hit 11 | 2 | 42 | 0.115 | 6 个下游 hits；station 2 主导 |

最大贡献者是磁 hop 之后的下游 hits，不是 station 0。下一步若继续查 86，应查这些 transport segments，**不要**再对整条轨迹做全局 FD 缩步。

## 9. 不允许的读法

1. **不是** derivative contract PASS。control 1 仍未过冻结 5%，86 无独立 reference。
2. **不是**可以因为 0.2% strip-sigma 效应就忽略 control 1 mismatch。
3. **不是** material reset 作为最高优先级机制。
4. **不是**某一个短 segment 内部 RK 与该段 FD 不一致（B），也不是 dumped-matrix composition 漏乘（C）。
5. **不是**已经为 86 建立了可信独立 reference（D）。Route 2 能拟合一个数 ≠ reference established。
6. **不是**可以继续缩 FD 步长，也不是可以增加新 rung 或挑最好 rung。
7. **不是**可以把 ACTS 矩阵写进正式 likelihood，也不是该改生产 `stepTolerance`。
8. **不是**本任务内部已经修好 Jacobian。A 只授权下一步修 `∂dir/∂pos`，然后重新做 contract。

## 10. 正式审计

Official run：`sbb14w_independent_reference_20260908T204204Z_d305d4b7`

| 文件 | SHA |
| --- | --- |
| `control1_loc1_missing_coupling.json` | `d319be91d7614773e39218b4d36e723b5bb1f4d97300d29d5a49af13b684bae0` |
| `material_variational_reset_audit.json` | `8af7ac77b8b76d04a9637facdc952560086fd6f41c7fd4e74b2f5bc9678d1c1c` |
| `segment_derivative_closure.json` | `1f64ec66ec6bcca48a1a2f451d947481676c1c77cf00cc6e477b2b2b9e277914` |
| `focus86_independent_derivative_reference.json` | `87b28ea66690c86736db519f5c4bc06cabec92ed2212967f8618b5166166b3c0` |
| `focus86_per_hit_derivative_breakdown.json` | `9125cfce334c1607695bcb97e002514d9382ce095f1b53723c594aed62059d6c` |
| `derivative_reference_decision.json` | `c6d761e95383a2404455652a1b0b1baa1003e0ca22fd61cf8d07a51fd456dce9` |
| `inherited_stage.json` | `4bd2066124818b703b91d9f79a5e50760a16224487f423a26b68e9a87f3a09cc` |
| `COMPLETE.json` | `354d53b7293432637fcc874662836e4d2ead8885497945490f40bc68181f6536` |
| config | `b68b826e7098d51338ce42e8613d250a09600bd9cfea4c852ec322b34a33d9c7` |
| helper | `bea305738d479f335fd64b1f7a92e0d9daf66a395873d17dda18bfa2e19927c3` |

`smoke_gate_passed = true` 只表示冻结 WB120 材料与五份诊断齐全，**不**表示 derivative contract 通过。

重新认证 derivative 的条件仍未满足：

```
control 0 PASS
control 1 PASS          ← 仍 FAIL（5.6–6.2%）
control 37 PASS
focus 86 has independent reference   ← false
official-path analytic agrees with reference
branch switching = false
target leakage = 0
5% gate unchanged
```

因此：

```
jacobian_contract_established = false
b14m_reopen_authorized = false
```

## 11. 冻结

```
decision = missing_deterministic_transport_coupling_identified
jacobian_contract_established = false
b14m_reopen_authorized = false
restart_invariance_authorized = false
full_sample_authorized = false
b15_authorized = false
measurement_model_v2_authorized = false
control1_station0_loc1_agrees = true
rk_magnet_hop_pos_to_dir_is_zero = true
material_reset_is_highest_priority_mechanism = false
focus_independent_reference_established = false
five_percent_gate_unchanged = true
do_not_relax_five_percent_gate = true
do_not_change_official_fd_ladder = true
do_not_add_fd_rung = true
do_not_shrink_fd_step = true
do_not_change_jacobian_implementation = true
do_not_replace_official_likelihood = true
do_not_use_dummy_cov_bounded_transportJacobian = true
do_not_select_best_step = true
do_not_select_best_tolerance = true
statistical_model_unchanged = true
small_physical_effect_does_not_pass_contract = true
next_step = repair_missing_pos_to_dir_variational_coupling_then_recontract
```

主线：

```
WB119 拿错了 Jacobian
  → WB120 给正式 supporting-plane likelihood 建立同路径 derivative
  → WB121 判断剩余 mismatch 是 FD 不够精确、small-column 病态，还是 analytic 仍有真实错误
      control 1 loc1 = 真实局部错误；86 = FD reference 不够精确
  → WB122 定位缺失 coupling + 尝试为 86 建立独立 reference
      ← 本任务：control 1 = 磁 hop ∂dir/∂pos 漏项；86 = 独立 reference 仍未建立
  → 修 missing pos→dir variational coupling
  → 重新做 control+86 derivative contract
  → 只有 PASS
  → reopen B14M smoke
  → restart invariance
  → only then consider full-sample profile campaign
```

下一步允许修 derivative implementation 中磁 hop 的 `∂dir/∂pos` / 场梯度 variational 项，然后重新做 control 0/1/37 + 86 contract。不允许改生产 `stepTolerance`、放宽 5% gate、增加 FD rung、缩小 FD 步长、用 truth q/p、加 prior / ridge、或把未验证的 ACTS 矩阵直接写进正式 likelihood。不重开 B14M，不提交 1989。86 在修好 control 1 之前，不得用 analytic 自我认证。
