# 2026-08-21 (52) 四站未知关联：冻结 V2 association-only 诊断

## 任务边界

条目 51 已完成代码审计并预注册闸门。本条目只在同一 four-station physical
bank 的三个 payload 上跑冻结 V2 + unit-capacity route solver：

- `iteration_00_reference`
- `iteration_00_closure_relative`
- `iteration_00_closure_relative_plus_common`

不扩 Athena 生产，不重训，不用 held-out 调 threshold / unmatched penalty /
calibration。密封 test 未打开。15 维 WLS **没有**开跑：预注册 vs-nominal
闸门未过。

## 两个 inference 域

冻结 V2 的 checkpoint、calibration 与 unmatched penalty `-1.0` 是在
**synthetic overlay**（约 3 轨/事件 + hard/easy fake）上选的。先在 identity
物理 ROOT 上打了一枪，再在同一 3 个物理点的冻结 overlay 配方上复打。后者
才是冻结 operating point 的域；前者用来证明「0 route」不是四站错位本身。

Overlay 配方：`configs/physical_alignment_iteration_trainval.yaml`
（`overlay_seed_scope: alignment_iteration_shared_across_payloads`，
`events_per_payload: 120` × 2 train source）。不是新的 Athena 生产。

## Identity 物理图（对照，不是冻结 OP 域）

| payload | raw chain recall | score-retained chains | selected routes | unique physical edges |
| --- | ---: | ---: | ---: | ---: |
| reference | 0.961 (74/77) | 74/77 | 0 | 0 |
| relative | 0.961 | 74/77 | 1（不完整） | 2 |
| relative+common | 0.949 | 74/77 | 1（不完整） | 2 |

Raw candidate 完整。冻结 score 阈值 0.001 仍留住几乎全部 truth chain。
Route packing 选出 ~0 条，是因为 identity 单轨事件上校准后分数约 0.6
（brier≈0.15，几乎没有负样本），边 logit 加 `4 × unmatched_penalty(-1)`
后 utility&lt;0，正 utility 子集为空。Nominal 同样为 0，**不是**四站
held-out 特有崩塌，也不是 candidate/propagation 失败。禁止因此去调
unmatched penalty。

## 冻结 overlay 域（正式 association 诊断）

Raw candidate（全部 payload）：

- 完整 truth-chain recall = **1.0**（473/473）
- 相邻 0→1 / 1→2 / 2→3 truth-edge recall = **1.0**
- 非相邻 0→2 / 0→3 / 1→3 ≈ 0.978–0.988

因此 **不是** propagation/candidate 层失败。

选路（冻结阈值 0.001、penalty −1.0、不调参）：

| 量 | reference | relative | relative+common | vs nom. |
| --- | ---: | ---: | ---: | ---: |
| complete-track efficiency | 0.903 | 0.712 | 0.706 | 降 0.190 / 0.197 |
| complete-track purity | 0.977 | 0.963 | 0.960 | 降 0.014 / 0.017 |
| track fake rate | 0.027 | 0.035 | 0.037 | 升 0.008 / 0.010 |
| selected routes | 564 | 570 | 572 | — |
| duplicate routes | 0 | 0 | 0 | — |
| unique physical edges | 236 | 236 | 236 | — |
| 选中边 0→1 / 1→2 / 2→3 | 542/528/459 | 545/523/375 | 547/524/373 | 2→3 掉 |
| 每站参与（边端点）0/1/2/3 | 542/1070/987/459 | 545/1068/898/375 | 547/1071/897/373 | S3 掉 |
| score-retained complete chains | 473/473 | 473/473 | 473/473 | 阈值未掉 |

相邻 association efficiency：

| pair | reference | relative | relative+common |
| --- | ---: | ---: | ---: |
| 0→1 | 0.896 | 0.901 | 0.905 |
| 1→2 | 0.886 | 0.877 | 0.879 |
| 2→3 | **0.793** | **0.639** | **0.634** |

Purity / fake 相对 nominal 过预注册闸（purity 降 ≤0.05，fake 升 ≤0.05）。
**Efficiency 相对 nominal 降 0.19，超过预注册 0.10。** 选中边仍含 0→1、
1→2、2→3，没有沿用 IFT「只留 station 0」过滤。score 阈值仍留住全部
truth chain；掉的是 **S3 相邻 2→3 被 route packing 选中的比例**，与
held-out 的 S3 `ry=5 mrad` 同侧。

预注册决定（`association_gate_decision.json`）：

- `raw_candidate_graph_complete`: true
- `frozen_v2_association_stable_vs_nominal`: false
- `continue_to_15d_relative_wls`: **false**
- `failure_class`: `association_domain_shift`
- 未打开 15 维 WLS，未放宽 `ΔT_ij` capture，未改 V2

## 分层结论

1. Candidate/propagation：通过（raw recall 1.0）。
2. 冻结 V2 score 阈值：通过（473/473 仍在 0.001 之上）。
3. 冻结 unit-capacity 选路：相对 nominal **未通过**，机制在 2→3 / S3。
4. 因此 **还不是** 本分支的正式四站未知关联 closure。按条目 51 合同，
   **现在才允许规划** 四站感知 GPU 重训；不要用这两个 held-out 调阈或
   unmatched penalty 把 efficiency 补回去，也不要假装 15 维几何已经恢复。

重训规划约束（尚未开工）：只 GPU；train/validation 按原始 xAOD 隔离；
密封 test 保持关闭；课程必须含四站同时可动的物理错位，而不是再复制
IFT-only / 纯平移 curriculum；第三个 train source 仍等到未知关联 closure
真正通过之后。

## 产物

- identity：`outputs/mc24_four_station_identifiability_pilot_v1/frozen_v2/`
- overlay：`.../frozen_v2_overlay/` 与 `frozen_v2_overlay_synthetic_v1/`
- 闸门 JSON：`frozen_v2_overlay/association_gate_decision.json`
