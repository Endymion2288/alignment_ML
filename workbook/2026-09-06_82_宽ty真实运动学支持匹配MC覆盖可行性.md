# Workbook 82: 宽 ty 真实运动学支持匹配 MC 覆盖可行性 V1

日期：2026-09-06
状态：**完成并冻结** —— 单一交互会话内按冻结顺序执行；所有 gate 在任何 confirmatory 计算前写入 config 并冻结，执行后未修改任何 gate。

**最终决策：`existing_mc_real_wide_ty_support_validated`**
**validated candidate：`floor_muon_100120`（floor-origin muon gun）**

> **本 Workbook 82 是 WB81 明确保留的独立支线（连续编号 82），不是 covariance repair。** covariance repair 属于 Workbook 83（`Propagated-Covariance Upstream Repair & MC Validation V1`），只有在 WB82 冻结后才允许启动。

本战役**完全 residual-blind**。整个 Workbook 82：

- `held_out_accessed = false`（不读任何 real-data residual，held-out 完全关闭）
- `real_data_alignment_authorized = false`
- `geometry_write_allowed = false`
- `official_conditions_write_allowed = false`
- `external_constraint_ingest_authorized = false`
- `measurement_model_validated = false`（本战役**不**触碰 measurement model）
- `residual_blind = true`（**不读** alignment residual / FD derivative / Jacobian error / singular value / rank / final-correction 表现）

## 1. 科学问题（唯一）

**现有非 sealed MC/control 是否有足够的 track kinematic coverage，可以覆盖 WB80/81 real calibration population 的 (0,1)/(0,2) `pred_tx,pred_ty` applicability domain，从而未来训练/验证一个不需要对 real tracks 外推的 conditional-J model？**

WB80 已证明 canonical 3-source construction subset 的 transfer support 失败（(0,1)=0.806、(0,2)=0.872，gate ≥0.90），并因此冻结 `jacobian_transfer_model_validated=false`。WB80 的失败是**特定 3-source subset** 的结论；本战役问的是一个更广的问题：**全部现有非 sealed MC/control 中，是否存在任何一个 candidate 能覆盖真实 calibration support**。本战役不是 alignment，不看 residual，不看 rank，不看 Jacobian error。

## 2. 起始状态审计（STEP 1，已完成）

- Workbook 81 freeze commit：`4721e84`（单一 freeze commit，含 code+config+docs+workbook+tests；artifacts 存于 EOS，不入 git）。
- WB81 最终冻结：`faseracts_propagated_covariance_not_calibrated`，机制分类 `overestimated_transported_fit_covariance`。
- 本战役 `load_config` 对 WB81 逐项 SHA 验证（不一致即 `ValueError` 拒绝）：
  - WB81 config `5a13b0cc7273546ebbee4268a853c32c527a68fd263eb82483f3d6e581d5f2b9`
  - `propagated_covariance_decision.json` `fd605f0ea93dd239a06c4ae8f14a619e1735709963d657b4c9a68575e9db96c2`
  - `campaign_summary.json` `97a92006163770ee3986a992813b3abb2a58843f7953f25134de1f8412645a4f`
- WB81 冻结决策与机制分类已逐字核对一致。

## 3. 冻结数据角色与禁止事项

- **Real support target**：直接继承 WB80 frozen calibration population（run 14973、14974），只读 residual-independent track/pair metadata（`source_tx/source_ty`、`pred_tx/pred_ty`、lever arm、station pair）。**不读 residual 值。**
- **冻结 real target domain**：pair type (0,1)、(0,2)（gated）；(0,3) 只有 2 pair，继续 report-only。
- **Primary kinematic**：source-tracklet frame `source_tx/source_ty`（对所有 candidate 与 real target 统一）；`pred_tx/pred_ty` 作 cross-check（见 §6）。
- **Sealed source 继续禁止访问**：`mc24_100116_00030_00039`、`mc24_100117_00030_00039`、历史 100012 test split `mc24_00020_00024` 被结构性排除（config 加载时检查，见 tests）。
- **旧 campaign 失败结论继续冻结**：100120 曾失败 physically-distinct admission、100130 曾失败 movable-station min_pairs，**这并不自动禁止它们作为 residual-blind coverage control**；反过来，它们在本战役 coverage 好也**不重写**此前 identifiability 结论。
- 禁止：用 coverage 结果选 candidate、看结果后扩大 primary feature-space gate、降低 0.90 门、现场改 generator、打开 held-out/sealed、求 real-data correction。

## 4. Real support target（`real_support_target.json`，residual-blind）

| pair | n_pairs | source_tx_p95 | source_ty_p95 | pred_tx_p95 | pred_ty_p95 | lever_arm_med (mm) | gated |
| --- | --- | --- | --- | --- | --- | --- | --- |
| (0,1) | 227 | 0.0635 | 0.0237 | 0.0653 | 0.0262 | 1907.6 | 是 |
| (0,2) | 149 | 0.0417 | 0.0128 | 0.0448 | 0.0133 | 3097.6 | 是 |
| (0,3) | 2 | 0.0293 | 0.0130 | 0.0247 | 0.0154 | 4287.6 | 否（report-only） |

真实 (0,1) 群体在 ty 上明显比 canonical MC 宽（real source_ty_p95≈0.024 vs canonical≈0.0097），且呈 +ty 不对称（约 87% track ty>0）。这正是 WB80 发现的 "wide-ty" gap。

## 5. Candidate inventory（kinematic metadata only）

系统性 inventory 5 个现有非 sealed candidate（admission 只用物理 metadata / kinematics）：

| candidate | kind | species | 来源 | prior status |
| --- | --- | --- | --- | --- |
| `canonical_hierarchical_v1` | propagation | muon | 18 个 hierarchical V1 source（100043/44/47/48） | WB80 3-source subset 失败 |
| `gaussian_theta_100012` | propagation | muon | 4 个 train source（00000-00019） | 100 GeV Gaussian-θ gun |
| `fluka2d_100116_100117_nonsealed` | propagation | muon | 8 个非 sealed FLUKA 2D source | 低能 FLUKA |
| `floor_muon_100120` | tracklet_truth_matched | muon | 10 个 export source | 曾失败 physically-distinct admission |
| `kshort_100130` | propagation | pion | 10 个 source | 曾失败 movable-station min_pairs |

## 6. 覆盖度结果（`coverage.json`）

**Primary gate：每个 gated pair type 要求 Mahalanobis 99% envelope fraction ≥ 0.90 且 density bin-occupancy(h=0.01) ≥ 0.90 且统计充足（≥30 pair、≥2 source）。** 两个 coverage metric 互为冗余：Mahalanobis 是 WB80-consistent 的 envelope，bin-occupancy 是 robust 的密度测度（防 "wide-but-sparse" / outlier-inflation）。已预注册 physical acceptance filter（|tx|,|ty|≤0.2）去除非物理 vertical-track outlier，使 Mahalanobis envelope 不被虚增。

| candidate | (0,1) maha | (0,1) dens | (0,2) maha | (0,2) dens | n(0,1) | n_src | species(p_med) | coverage_gates_pass | particle_domain |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| canonical_hierarchical_v1 | 0.841 | 0.881 | 0.940 | 0.960 | 1431 | 18 | muon(486 GeV) | **否**（(0,1) 双败） | 兼容 |
| gaussian_theta_100012 | 0.749 | 0.529 | 0.893 | 0.651 | 17 | 4 | muon(100 GeV) | **否**（统计不足+双败） | 兼容 |
| fluka2d_100116_100117 | 0.877 | 0.767 | 0.980 | 0.906 | 65 | 8 | muon(54 GeV) | **否**（(0,1) 双败） | 兼容 |
| **floor_muon_100120** | **0.943** | **0.965** | **0.993** | **1.000** | **36123** | **10** | **muon(265 GeV)** | **是** | **兼容** |
| kshort_100130 | 0.885 | 0.700 | 0.980 | 0.832 | 43 | 10 | pion 211(113 GeV) | **否**（density 败） | **不兼容** |

**frame cross-check（`crosscheck_pred_frame_fraction_within_mahalanobis_99`）**：对所有 muon candidate，tracklet-frame 与 pred-frame 覆盖度一致（差 ≤0.03，如 canonical (0,1) 两 frame 均 0.841），验证 muon 的 source-slope frame 是 pred frame 的良好代理，从而 100120 的 tracklet-frame 覆盖度（无 propagations 可用）可信。pion（100130）两 frame 差异大（(0,1) 0.93 vs 0.78），正说明低动量 pion 的 frame 不可混用，但其 species 已不兼容。

**lever arm / origin 审计**：所有 candidate 与 real target 的 source-z（−1860.15 mm，station-0）与 lever arm（(0,1) 1907.6 mm）完全一致——floor-origin 只改变 (tx,ty) 分布，不改变 station-pair 几何 lever arm，故不影响 conditional-J 的几何依赖。

## 7. 决策（`wide_ty_mc_support_decision.json`）

按预注册 decision tree：

- `floor_muon_100120` 在 (0,1)、(0,2) 两个 gated pair type 上同时通过 Mahalanobis + density + 统计 gate，且 particle-domain 兼容（muon，265 GeV，lever arm 匹配）。
- 其余 candidate 均在 (0,1) 失败；`kshort_100130` 另因 species 不兼容（pion）被标记。

**决策：`existing_mc_real_wide_ty_support_validated`**
- `validated_candidates = ["floor_muon_100120"]`
- `real_kinematic_jacobian_support_validated = true`
- `conditional_j_retrain_permitted = true`（仅指**未来另开** J-retrain campaign 的 kinematic-support 前提成立）
- `new_mc_generation_required = false`
- `measurement_model_validated = false`（**不变**；还需 WB83 covariance gate）
- `real_data_alignment_v2_preregistration_allowed = false`（**不变**）

## 8. 结论与边界

1. **存在一个现有非 sealed MC（floor-origin muon 100120）覆盖真实 wide-ty calibration support**，(0,1)/(0,2) 双过 0.90 门，统计充足（36k pair、10 source，足够未来 construction/validation split），species/momentum/lever-arm 兼容。这推翻了 "必须新生成 MC" 的默认假设——**不需要新生成 MC**。
2. **canonical hierarchical V1（18 source）在 (0,1) 仍失败**（maha 0.841、density 0.881 双败），与 WB80 的 3-source 结论方向一致；canonical 的 ty 支持确实太窄。
3. **kshort_100130 是 pion**，即使角度覆盖部分真实支持，也标记 `particle_domain_mismatch`，不能用于 muon conditional-J。
4. **本战役不重写任何旧结论**：100120 的 physically-distinct admission 失败、100130 的 movable-station min_pairs 失败、WB80 的 measurement-model 失败、WB81 的 covariance 未校准，全部继续冻结。WB82 只回答 kinematic coverage 一个问题。
5. **汇合条件**：未来只有 `propagated_covariance_model_validated == true`（WB83）**且** `real_kinematic_jacobian_support_validated == true`（本战役已建立 candidate）才允许新开 Measurement Model Reconstruction & Validation V2。当前 covariance gate 仍未满足，**不能直接跳回 real-data alignment**。

## 9. 后续

- **Workbook 83**（`Propagated-Covariance Upstream Repair & MC Validation V1`）：先做 source-tracklet covariance truth closure（Stage A），再做 propagation covariance construction（Stage B），q/p covariance semantic repair 是独立合同。WB82 不修改任何 FaserActs covariance。
- 未来 conditional-J retrain campaign（另开预注册）可使用 `floor_muon_100120` 作为 kinematic-support-matched 训练/验证样本；其 propagations（pred frame）需在该 campaign 中生成并重新验证 frame 一致性。
