# Workbook 91: T03 Geometry / SE(3) 坐标契约

日期：2026-09-06
状态：**完成** —— 只建显式 chart API 与几何 fixture，不写 SQLite/POOL，不改冻结 campaign 输出。

**最终判定：`PASS`**

- `geometry_write_allowed = false`
- 混合角 build→extract→build 范数 `< 1e-10`
- 旧 survey `extract_alpha_beta_gamma` **不是**一般逆
- cluster-local Ry 已命名为独立旧 chart

## 1. 科学问题

physical payload、cluster-local Jacobian 和 survey 不能 Implicit 混用不同
coordinate chart。

## 2. 契约

Stations：主动左乘 `T * Rz * Ry * Rx`，FASER 原点，mm/rad。

中心 pivot：`t_origin = t_center + (I−R)c`，Jacobian/prior 一起变换。

Legacy cluster-local：Ry 反号，绕 `(0,0,station_z)`。不能复制为 Stations `ry`。

## 3. 产物

Run ID：`t03_alignment_chart_20260906T125929Z_4e748115`

Config SHA：`639e1ba25964f45952c7a5590a476563066ef33ecccde8fdb1b31834cae8c05c`

`alignment_chart_contract.json` SHA：`1ee727abb4424acc759c4e89e74980910ef39c869fae36d3d89948da271e4d58`
