# Workbook 88: T00 证据链与可复现清理

日期：2026-09-06
状态：**完成** —— 只做 metadata / hash 核对，无 production、无 sealed-test 读取、无历史负结果改写。

**最终判定：`PASS`**

审查基准远端 master `0c8a6ca34c6c8b4f505dc153dd0ca8cc7949ce56`。当前 HEAD 在该基准之后 20 个 commit（WB75–87 及审查文档）。T00 记录 diff 与 lineage，不把 WB75–87 倒填进 `PROJECT_MASTER_AUDIT.md`。

- `held_out_accessed = false`
- `real_data_alignment_authorized = false`
- `geometry_write_allowed = false`
- `measurement_model_validated = false`
- `production_ran = false`

## 1. 科学问题

不同代码、数据和 conditions 的实验不能拼成一条证据链。T00 只回答：审查列出的 E01–E09 是否仍在盘上、哈希是否与 2026-09-05/06 审查一致，以及当前树相对该基准的状态是什么。

## 2. 入口区分

| 入口 | 含义 |
| --- | --- |
| inference | 冻结 V2 / route packing，只作控制 |
| paired-response analysis | 同事件 FD residual WLS，诊断而非可部署 alignment |
| real-data monitoring | residual/DQ；禁止写 geometry |

## 3. 核对结果

Config SHA：`aac118f97795f929db0245c19c3f2a3feb1c9df2848c1078b8621c8629d6a420`

| 产物 | SHA256 |
| --- | --- |
| `outputs/master_audit_reconciliation_v1/evidence_registry.json` | `effa92bd0076b46b11157ca295a64f78692e09746b282afc9374e1c09a7b584c` |
| `outputs/master_audit_reconciliation_v1/claim_reconciliation.json` | `ac93b74427ea8af82b08317cc270852f4c13c80aa37668f7ae0f27f674fc8495` |
| `outputs/master_audit_reconciliation_v1/external_runtime_manifest.json` | `2be8b830acaf4eaa750a7e14d26ba6c33a311a7796863ccaa3c2da70a42cec11` |
| `outputs/master_audit_reconciliation_v1/e09_file_rehash.json` | `d707e022f2697f7e554ffef4697dd80a6d69d44158a613e4cbc7ff24c234437d` |

- 10 项登记证据（E01–E09 + E02 arrays）全部 `PASS`
- E09 manifest 内 13 个文件全部 `PASS`（含 frozen V2 checkpoint `0c85a001…`）
- Calypso/Athena/ACTS 针脚已记录，并标 `historical_runtime_unverified=true`
- 未打开 sealed test，未改冻结负结果数值

## 4. Finding 状态（不重解释）

仍开放，留给 T01–T03 / T10–T12：

- F01 bootstrap boolean mask
- F02 paired-reference oracle
- F03 相对 rank ≠ 结构性零空间
- F04 cluster-local chart
- F06 非 SPD chi2
- F07 欠定 SVD null
- F09 可覆盖报告

F05 covariance/transport：审查后已由 WB81–87 调查，**不是 T11 pass**。冻结负结果仍是 `faseracts_transport_covariance_not_validated`。

Post-audit lineage（WB81–87）已登记且全部 `backfill_into_master_audit=false`。

## 5. 下一步

进入 T01（access scope + 不可覆盖 artifact）。不训练，不跑大规模实验。
