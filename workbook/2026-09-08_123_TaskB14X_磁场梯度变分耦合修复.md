# Workbook 123: Task B14X Magnetic-Field-Gradient Variational Coupling Repair and Recontract

日期：2026-09-08
状态：**完成 / FAIL** —— 以 GitHub `master` `55cf982302a3c62c57b74f368d5e3ba7723fd33a` 为 checkpoint，以 **WB122** 为最新冻结。在**不改变** WB114 measurement likelihood、不改生产 `stepTolerance=1e-4`、不改变正式 `h,h/2,h/4,h/8` FD ladder、不增加 FD rung、不缩小 track-state FD 步长、不放宽冻结 5% gate、不按结果挑最好数值、不把正式 sequential likelihood 换成 direct-from-source、不加 prior / ridge、不删 `100043/37` 或 `100048/86`、不用 truth q/p、不调 Q / Cin、不以修 5D Cin 为目标的前提下，只修 **diagnostic variational derivative**。正式 mean `h_i(θ)` 一字不动。未重开 B14M，未做 restart invariance，未进 B15，未进 V4 C/D，未进 Measurement Model V2。未覆盖 WB109 / WB114–WB122 / `b14u_smoke` dumps。**未提交 1989 行 Condor**。禁止把 dummy-cov bounded `transportJacobian` 当作正式导数。禁止按 track Jacobian 符合度回调 field-gradient FD 步长。

**最终判定：`FAIL` / Case B**

- `decision = field_gradient_coupling_repaired_focus_reference_unresolved`
- `primary_case = field_gradient_coupling_repaired_focus_reference_unresolved`
- `smoke_gate_passed = true`
- `missing_term_source_proven = true`
- `mean_path_unchanged = true`
- `control_0_pass = true`
- `control_1_pass = true`
- `control_37_pass = true`
- `control_1_old_pos_to_dir_zero = true`
- `control_1_new_pos_to_dir_nonzero = true`
- `focus_independent_reference_established = false`
- `jacobian_contract_established = false`
- `b14m_reopen_authorized = false`
- `restart_invariance_authorized = false`
- `full_sample_authorized = false`
- `target_exclusion_holds = true`（12 行，0 leaked）
- `prior_introduced = false`
- `ridge_added = false`
- `statistical_model_unchanged = true`
- `five_percent_gate_unchanged = true`
- `b15_authorized = false`
- `measurement_model_v2_authorized = false`
- `next_step = keep_86_independent_segment_reference`

这仍然不是 B14M PASS，也不是 Jacobian contract PASS。WB122 已经证明磁 hop 的 `rk_free ∂dir/∂pos ≡ 0`。本任务从 pinned ACTS/FASER 源码证明缺失项是场梯度 tangent，并在**只修诊断变分路径**后把 `100043/1` 的 0.001–0.002 loc1 coupling 解释掉；control 0/37 保持冻结 5% PASS。`100048/86` 的 hop-start segment FD 仍无法建立独立 reference，因此不能因为修复后 analytic 稳定就自我认证。5% gate 未改。未重开 B14M。未提交 1989。

不允许把本跑判成 Case A。不允许因为 control 1 已经修好就重开 B14M。不允许继续缩 track FD 或增加 rung 来“过”86。不允许改正式 mean / 测量模型 / 生产 `stepTolerance`。

## 起始状态

HEAD：`55cf982302a3c62c57b74f368d5e3ba7723fd33a`

WB122 FAIL：`missing_deterministic_transport_coupling_identified`  
Official run：`sbb14w_independent_reference_20260908T204204Z_d305d4b7`  
Decision SHA：`c6d761e95383a2404455652a1b0b1baa1003e0ca22fd61cf8d07a51fd456dce9`  
Config SHA：`b68b826e7098d51338ce42e8613d250a09600bd9cfea4c852ec322b34a33d9c7`  
Helper SHA：`bea305738d479f335fd64b1f7a92e0d9daf66a395873d17dda18bfa2e19927c3`

```
decision = missing_deterministic_transport_coupling_identified
jacobian_contract_established = false
b14m_reopen_authorized = false
five_percent_gate_unchanged = true
control1_station0_loc1_agrees = true
rk_magnet_hop_pos_to_dir_is_zero = true
material_reset_is_highest_priority_mechanism = false
focus_independent_reference_established = false
new_hop_start_segment_fd_computed = false
next_step = repair_missing_pos_to_dir_variational_coupling_then_recontract
```

WB122 已冻结：

```
100043/1 station-0:
FD == rk_free

first mismatch:
hit 6 = first long magnetic hop

long magnetic hop:
rk_free ∂dir/∂pos ≡ 0

mean finite difference:
nonzero position → direction coupling

subsequent 0-step hits:
inherit this missing coupling

material reset:
not primary mechanism

matrix composition:
closed

100048/86:
independent derivative reference still NOT established
```

冻结 likelihood 不得改写：

```
theta = (loc0, loc1, phi, theta, q/p)
alpha = (loc0, theta)
nu    = (loc1, phi, q/p)
chi2(θ) = Σ r_i(θ)^T R_i^{-1} r_i(θ)
R_i = (0.08 mm)² / 12
```

WB103 冻结 denominator 保持：raw 2680 / ineligible 691 / contracted 1989 / official pairs 1974。

冻结 FD ladder 保持：

```
h(loc0)=0.01 mm, h(loc1)=0.01 mm, h(phi)=1e-5, h(theta)=1e-5, h(q/p)=1e-6 /GeV
rungs: h, h/2, h/4, h/8
last-pair rel ≤ 0.05 and sign consistent
production stepTolerance = 1e-4
```

## 1. 本任务唯一问题

不是重新寻找 Jacobian，而是回答：

> ACTS/FASER magnetic propagation 的 deterministic tangent map 中，是否缺失由空间非均匀磁场产生的 `position → direction` derivative；如果是，正确补齐它以后是否能够解释 `100043/1` 的 0.001–0.002 coupling，并为 86 建立可独立验证的 derivative contract？

范围严格是 `100043/0,1,37` 与 `100048/86`。不是 44。不是 1989。不重开 B14M。

## 2. 源码：必须先钉死 ACTS tangent，才能 patch

Pinned stack：

```
ACTS 32.0.2
AthenaExternals 24.0.41
EigenStepper
GenericDefaultExtension::transportMatrix
FASERMagneticFieldWrapper
```

Mean ODE（`EigenStepper.hpp` / `.ipp`）：

```
dr/ds = T
dT/ds = (q/p) T × B(x)
```

RKN4 stage 位置：

```
pos0 = x
pos1 = x + (h/2) T + (h²/8) k1
pos2 = x + h T + (h²/2) k3
B_first at pos0, B_middle at pos1, B_last at pos2
k = qop * T_stage × B(x_stage)
kQoP = {0,0,0,0}
```

`transportMatrix` 在 `pars` 更新之前调用，因此 `position()` / `direction()` 是 step-start。

`GenericDefaultExtension::transportMatrix` 明确：

- `D = Identity`
- 只填 `dFdT`, `dFdL`, `dGdT`, `dGdL`, `D(3,7)`
- 注释：*“Since the terms of eq. 18 are currently 0, this matrix is not needed”*（ATL-SOFT-PUB-2009-002）
- 注释：*“dGdx is already initialised as (3x3) zero”*
- 从不调用 `getFieldGradient`

因此累积 `rk_free` 有 `∂dir/∂pos ≡ 0`。

必须区分、不得混写：

```
uniform-field tangent     ← ACTS D 已有
field-gradient tangent    ← 缺失；本任务只修这一项
energy-loss deterministic ← 默认 extension 的 k 里没有
MS / process noise        ← 不在 D
covariance transport      ← dummy cov 只是变分开关
```

`missing_term_source_proven = true`。

官方 gradient API 已存在，不发明 field-query FD 作为官方 `G`：

```
FASERMagneticFieldWrapper::getFieldGradient
fieldCache.getField(pos, B, grad)
bfield   *= 1000 * Acts::UnitConstants::T   # kT → ACTS T
gradient *= 同一因子                        # Tesla / mm
```

预注册的 field-query FD（只诊断官方 `G`，不是 track-state FD）：

```
field_gradient_fd_step_mm = 1.0
justification: magnet-region mesh is O(10 mm); 1 mm is inside a cell
do_not_tune_from_track_jacobian = true
```

120 个 RK 采样点：`B` 重复查询差 = 0；官方 `G` 与 1 mm field-query FD 的 Frobenius 中位 5.1e-9，最大 1.1e-5。步长未按 track Jacobian 回调。

同单位 tangent 从 pinned mean RKN4 推导，不是按示意 ODE 手写系数：

```
dk = qop * (dT_stage)×B + qop * [T_stage]_× * G * dx_stage
```

`G = 0` 时 `D` 必须退回 ACTS `GenericDefaultExtension::transportMatrix`。不加入能量损失 tangent、process noise、方向归一化 Jacobian。

## 3. 实现：只改 diagnostic variational path

Config：`configs/field_gradient_variational_repair_v1.yaml`  
SHA：`c290594f834233ace2c41d2d339edaa6fd9a4b6b88dc781ece67e014a012906b`

新诊断 stepper：

```
using GradientStepper =
    Acts::EigenStepper<Acts::StepperExtensionList<FieldGradientDefaultExtension>>;
```

`FieldGradientDefaultExtension` 的 `k()` / time 与 ACTS 默认 extension 相同。`transportMatrix` 在同一三个 stage 位置调用官方 `getFieldGradient`。正式 `EigenStepper<>` / 正式 mean `h_i` 不动。dummy cov 仍然只打开诊断 hop 的 `covTransport`。官方 start covariance 仍是 `nullopt`。

Hop-start segment FD 用**正式 mean EigenStepper**（不是修复后的 stepper），从真实 hop-start state 出发，冻结 `±h…±h/8`。每个臂只对**第一条** `n_steps > 10` 的长 hop 做 segment。禁止 analytic 自我认证。

Helper SHA 相对 WB122 改变（预期）：`ff74be09335bd6c63cd1357570d4623563cef7a3bca921909a456c0f5208bbf2`  
Extension：`alignment/leave_target_out_dump/FieldGradientDefaultExtension.hpp`  
SHA：`ca5e4f0ef1a7a09edd1c24099e7ce689c4f0511e2d4afab3a160ebd2d6ff03d8`

测试：`tests/test_field_gradient_variational_repair.py` + `tests/test_independent_derivative_reference.py` 共 14 passed（login ML env，无 Athena）。86 的独立 reference 必须钉在 WB122 的 first-unstable hop（target 1/2/3 = hit 6/11/11）；更早一条已经收敛的长 hop 不能冒充 required hop。

## 4. Smoke

Athena smoke：`scripts/run_field_gradient_variational_repair_smoke.sh`  
只跑 `100043/0,1,37` 与 `100048/86`。未覆盖：

| dump | 本任务后 | 冻结哈希 / 大小 |
| --- | --- | --- |
| WB109 official `ckf_leave_target_out.jsonl` | 未覆盖 | 1951622 bytes |
| WB120 `b14u_smoke` 100043 | 未覆盖 | `1c2daf7fda4e585d62624d1705934c3d621a69d560881f3d417d8a96bb1343ab` |
| WB120 `b14u_smoke` 100048 | 未覆盖 | `831a2a3fc9ff6e0aa519895d642a5de576c42a3a947700fab48df58bb798a19d` |

新诊断 dump（`b14x_smoke`）：

| dump | bytes | SHA | lines |
| --- | --- | --- | --- |
| `mc24_100043_00400_00499/ckf_leave_target_out_official_jacobian.jsonl` | 2848041 | `b3a52180bb4f211b4352ee41f71762a8c159fe66e8291087ca14791f11f56582` | 9 |
| `mc24_100048_00000_00049/ckf_leave_target_out_official_jacobian.jsonl` | 928782 | `d241fa360246ab8afc5796a0f26973f8ee00081f764f918463ffdcbfd4dcf1f2` | 3 |

12 行正式 Mode B：`evaluate_only` + `init_kind=wb114_official_seed_mean` + supporting-plane + `hop_mode=sequential_z_order`。

## 5. Mean-path invariance

全部 12 行：

```
field_gradient_mean_loc0_unchanged = true
max_abs_mean_loc0_delta = 0.0
chi2 来自正式 mean，未因诊断 stepper 改写
```

正式 `predicted_loc0` / 正式 `chi2` 在数值容差内完全一致。没有改 measurement prediction、material model、field map、生产 `stepTolerance`。

## 6. Control 1：最关键的证伪

必须先看 `100043/1`，不要先看 86。

第一条长磁 hop：旧 `∂dir/∂pos = 0`，新 ≠ 0。

| event/target | first long hit | steps | old p2d | new p2d |
| --- | --- | --- | --- | --- |
| 0 / 1 | 6 | 93 | 0 | 1.16e-7 |
| 0 / 2,3 | 6 | 72 | 0 | 5.85e-8 |
| **1 / 1** | **6** | **100** | **0** | **2.46e-6** |
| **1 / 2,3** | **6** | **75** | **0** | **1.16e-6** |
| 37 / 1 | 5 | 68 | 0 | 1.18e-7 |
| 37 / 2,3 | 5 | 51 | 0 | 8.74e-8 |

Event 1 采到更强的场图空间依赖（new p2d 约为 event 0 的 20 倍），与 WB122 一致。

`100043/1` loc1，冻结 `h` 列，相对误差：

| target | loc1 old-vs-FD | loc1 new-vs-FD | sign |
| --- | --- | --- | --- |
| 1 | **6.161% FAIL** | **0.016% PASS** | consistent |
| 2 | **5.638% FAIL** | **0.035% PASS** | consistent |
| 3 | 2.728% | **0.008% PASS** | consistent |

五个 bound 列全部也过 5%。Phi 在 event 1 上略差（~0.32% vs ~0.15%），仍 ≪ 5%。没有挑最好数值。

target 1 逐 hit loc1（residual column = −dloc0/dsource）：

| hit | J_FD | J_rk_old | J_rk_repaired | ΔJ_old | ΔJ_new |
| --- | --- | --- | --- | --- | --- |
| 5 (station 0) | −0.019999 | −0.019999 | −0.019999 | ~0 | ~0 |
| 6 (first magnet) | 0.019161 | 0.019999 | 0.019164 | +0.000838 | +3e-6 |
| 12 | 0.018178 | 0.019999 | 0.018174 | +0.001820 | −4e-6 |
| 17 | −0.022067 | −0.019999 | −0.022072 | +0.002068 | −6e-6 |

这就是 WB122 的 0.001–0.002 extra coupling。修复后的 field-gradient `∂dir/∂pos` 解释了它；后续 0-step hits 通过 continuation 继承。不要只比较最终 column norm。

Hop-start segment FD（正式 mean stepper，同一 hop-start state）对 control 1 hit 6：

```
target 1: repaired hop dloc0/d loc1 = -0.039117
          segment FD h…h/8         = -0.039124, -0.039102, -0.039132, -0.039142
          converged = true, agree = true
```

target 2/3 同样 converged + agree。这把 source→many-hop composition 与单段磁 hop 分开：该段的物理 transport map 的导数就是修复后的 tangent。

## 7. Control 0 / 37 必须保持原冻结 gate

| event | targets | all five columns vs frozen FD |
| --- | --- | --- |
| 100043/0 | 1/2/3 | PASS（loc1 new-vs-FD 0.039%, 0.049%, 0.003%） |
| 100043/37 | 1/2/3 | PASS（loc1 new-vs-FD 0.158%, 0.143%, 0.075%） |

所有 control 第一条长 hop 的 hop-start segment FD 都收敛，并与修复后 hop loc1 在 5% 下一致。若修 `∂dir/∂pos` 后破坏 0/37，必须停在 Case D。没有发生。

## 8. Material 只作 secondary check

WB122 冻结：`material_reset_is_highest_priority_mechanism = false`。本任务不改 Q，不把主线改回 material。

磁 hop 上：

- 旧 last-reset `∂dir/∂pos = 0`
- 新 last-reset `∂dir/∂pos` 非零但很小（剩余短程，例如 control 1 hit 6：1.4e-8）
- 新完整 RK 积保持非零 `∂dir/∂pos`（2.5e-6）

curvilinear / material reset **没有**把新加入的 `∂dir/∂pos` 从累积积里清掉。后续 0-step hits 继续继承。只做 continuity check。

## 9. 86：必须先有 control 1 PASS，才能谈独立 reference

WB122 已证明 86 的全局 FD `h,h/2,h/4,h/8` 振荡 / 变号，不能继续当唯一 reference。本任务要求两条互相独立的证据：

```
Reference A = repaired tangent（ACTS mean + official G + repaired ∂dir/∂pos）
Reference B = hop-start segment FD（同一 hop-start，冻结四档）
要求 A ≈ B，冻结 5% gate
禁止 analytic 与自己比较
```

Required first-unstable hops（WB122 冻结，不是“第一条任意长 hop”）：

| target | required hit | steps | old p2d | new p2d | \|G\|_F max | \|B\| max | q/p→dir | hop-start FD | loc1 收敛 | A≈B |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | **6** | 128 | 0 | 4.17e-4 | 7.0e-6 | 1.72e-4 | 0.479 | **有** | **否** | **否** |
| 2 | **11** | 80 | 0 | 4.65e-4 | 6.3e-5 | 2.46e-4 | 0.290 | **无** | — | **否** |
| 3 | **11** | 42 | 0 | 4.16e-4 | 6.7e-5 | 2.64e-4 | 0.193 | **无** | — | **否** |

Target 1 hit 6 是本任务实际算出的 required hop-start FD：

```
repaired hop dloc0/d loc1 = -0.06991
segment FD h…h/8         = -0.07850, -0.11748, -0.02478, -0.05634
sign_change = false, last_pair_rel = 0.560  (≫ 5%)
phi 变号；q/p 变号
```

因此 `independent_reference_established = false`。修复后 analytic 稳定 **不能**自我认证。

补充（不能替代 required hop）：targets 2/3 的**第一条**长 hop 是 hit 6（84 steps），不是 first-unstable hit 11。该 hop 的 segment FD **收敛并且与 repaired hop loc1 一致**（−0.04365 vs −0.04345…−0.04379）。这只说明有些磁 hop 可以被独立验证；不能把这条已经稳定的 hop 当成 86 的独立 reference。

全局 FD 在修复后对 loc1 大幅靠近，但仍过不了 5%，且四档仍振荡：

| target | loc1 old-vs-FD | loc1 new-vs-FD | 全局 loc1 四档仍振荡 |
| --- | --- | --- | --- |
| 1 | 97.6% | 6.0% | 是（signed norms 0.66, 1.57, 0.90, 0.17） |
| 2 | 98.3% | 18.0% | 是 |
| 3 | 81.3% | 26.7% | 是 |

**86 原来全局 FD 振荡，是否主要来自缺失的 field-gradient tangent？**

不是“已经全部解释”，也不是“场梯度假说不成立”。缺失的 `∂dir/∂pos` 是 control 1 的机制，并且显著减小了 86 的 loc1 全局偏差。但 required magnet segment 上，冻结四档 hop-start FD 仍然不收敛 / 未计算，所以修复后**仍然存在另外的 numerical derivative reference 问题**。不能用 Case A 收口。

`focus_independent_reference_established = false`。

## 10. 不允许的读法

1. **不是** Case A / Jacobian contract PASS。86 没有独立 segment reference。
2. **不是** Case C。源码证明缺 `∂B/∂x`；control 1 的 0.001–0.002 coupling 被同单位 tangent 解释，且 hop-start FD 与 repaired hop 一致。
3. **不是** Case D。control 0/37 保持 PASS；control 的 hop-start FD ≈ repaired tangent。
4. **不是**可以因为 analytic 变稳定就为 86 自我认证。
5. **不是**可以把 targets 2/3 已收敛的 hit 6 当成 first-unstable reference。
6. **不是**可以放宽 5%、增加 rung、缩小 track FD、改生产 `stepTolerance`、用 truth q/p。
7. **不是**已经可以重开 B14M 或提交 1989。
8. **不是** material / Q / Cin 主线。
9. **不是**正式 mean / 正式 likelihood 被改写。`predicted_loc0` 与 `chi2` 前后一致。
10. **不是**可以把诊断 stepper 的 Jacobian 写进正式 likelihood。

## 11. 正式审计

Official run：`sbb14x_field_gradient_repair_20260908T235722Z_f86bd896`

| 文件 | SHA |
| --- | --- |
| `acts_field_gradient_variational_source_audit.json` | `26281ef763676af542703e990c56e9ea7a550e6e26cd94ff64418f03c1ba8c86` |
| `field_gradient_variational_equation_contract.json` | `49e31d27950bf4b72e36f0b904ea0b5f0fc244ecaa369fab609e4fca69ddd4d2` |
| `magnetic_field_gradient_contract.json` | `ccc51a9e085849b1da95d1d6a437f02dbf3d5982a25493e916dc9526e445548f` |
| `field_gradient_variational_repair_invariance.json` | `c061021b3b0e6a3fb54f2d1bf573f64680fb814f48df4513e37a7ffb645ef373` |
| `control1_field_gradient_coupling_repair.json` | `3b4652c1ad85bdc9c320d5fb042d2963617dced8d1d2b9a9754d1ff530874301` |
| `long_magnet_segment_fd_contract.json` | `114a82294694fa8f55c4ead2702f94f6c130ecd25466600452ee46570a73e2d4` |
| `focus86_repaired_derivative_reference.json` | `63920dd96394bd44a32b0b639ec588dd4c1b7a67b7abd2dbb77c7c4705c72ff9` |
| `material_tangent_continuity_check.json` | `e7f986af833aac0e6ab2545741159f6eb417d580968dae2b5adffeb0410c6e29` |
| `field_gradient_variational_decision.json` | `4632686ec5e68f4d30470438d257c50ec7056edf430ea2460453052fd0b995aa` |
| `inherited_stage.json` | `46046986d886412a8dcf95160823339382a91624eb39fb96ee732a569ffc282f` |
| `COMPLETE.json` | `74929a4c62606b8d299998cb3bb7e73a1eed0c4b1d9c566158b57c4282509dfc` |
| config | `c290594f834233ace2c41d2d339edaa6fd9a4b6b88dc781ece67e014a012906b` |
| helper | `ff74be09335bd6c63cd1357570d4623563cef7a3bca921909a456c0f5208bbf2` |

`smoke_gate_passed = true` 只表示源码审计、方程契约、12 行 dump 与冻结 gate 材料齐全，**不**表示 Jacobian contract 通过。

重新认证 derivative 的条件仍未满足：

```
control 0 PASS          ← true
control 1 PASS          ← true
control 37 PASS         ← true
mean path unchanged     ← true
86 segment independent reference   ← false
repaired analytic agrees with that reference
5% gate unchanged
```

因此：

```
jacobian_contract_established = false
b14m_reopen_authorized = false
```

## 12. 冻结

```
decision = field_gradient_coupling_repaired_focus_reference_unresolved
jacobian_contract_established = false
b14m_reopen_authorized = false
restart_invariance_authorized = false
full_sample_authorized = false
b15_authorized = false
measurement_model_v2_authorized = false
missing_term_source_proven = true
mean_path_unchanged = true
control_0_pass = true
control_1_pass = true
control_37_pass = true
focus_independent_reference_established = false
material_reset_is_highest_priority_mechanism = false
five_percent_gate_unchanged = true
do_not_relax_five_percent_gate = true
do_not_change_official_fd_ladder = true
do_not_add_fd_rung = true
do_not_shrink_fd_step = true
do_not_change_production_step_tolerance = true
do_not_change_official_mean_path = true
do_not_tune_field_step_from_track_jacobian = true
do_not_mix_process_noise = true
do_not_replace_official_likelihood = true
do_not_use_dummy_cov_bounded_transportJacobian = true
statistical_model_unchanged = true
small_physical_effect_does_not_pass_contract = true
next_step = keep_86_independent_segment_reference
```

主线：

```
WB122:
missing coupling identified
magnetic long hop: ∂dir/∂pos incorrectly zero
        ↓
WB123:
prove field-gradient tangent term          ← 完成
+
repair derivative-only variational path    ← 完成；mean 不变
+
segment-start FD falsification             ← control 完成；86 required hop 未建立
        ↓
control 0/1/37 derivative contract         ← PASS
        ↓
86 independent segment reference           ← 未建立
        ↓
Case B: 下一任务只处理 86 numerical reference
        ↓
ONLY IF 86 reference + A≈B PASS
jacobian_contract_established = true
        ↓
reopen B14M smoke
        ↓
restart invariance
```

下一步只处理 86 的独立 numerical reference：在**不增加 track FD rung、不缩小 track FD、不改生产 `stepTolerance`、不放宽 5%、不用 analytic 自我认证**的前提下，为 required first-unstable hops（尤其 target 1 hit 6，以及尚未做 hop-start FD 的 target 2/3 hit 11）建立可收敛的 segment-level reference。不重开 B14M，不提交 1989。
