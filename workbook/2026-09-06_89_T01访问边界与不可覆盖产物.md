# Workbook 89: T01 访问边界与不可覆盖产物

日期：2026-09-06
状态：**完成** —— 只做 access / writer 契约，无 sealed-test 读取，无 production。

**最终判定：`PASS`**

- `held_out_accessed = false`
- `geometry_write_allowed = false`
- `frozen_evaluation_unseal_granted = false`
- `--allow-sealed-test` 不是开发许可

## 1. 科学问题

研究入口不能误开 sealed 数据，报告重跑不能覆盖冻结结论。

## 2. 实现

四类 scope：`train` / `development_validation` / `real_data_monitoring` /
`frozen_evaluation`。T00–T12 全部 fail closed，不解析 test path。

三条 legacy 默认保护保留，并接到同一策略：

- global-assignment `validation_only=True` → train+validation
- station-pair `evaluate_test=False` → train+validation
- curriculum MLP 无 flag 仍拒绝；有 flag 也不是开封许可

新 report 使用 `ImmutableArtifactStore`：唯一 run ID、独占目录、原子 finalize。

## 3. 判定

`pytest`：`test_sealed_access_policy` + `test_immutable_artifacts` + `test_expanded_control_boundaries` + `test_physical_curriculum_manifest` = 24 passed。

Run ID：`t01_access_lifecycle_20260906T125207Z_953bef38`

Config SHA：`eaa9d76c3e2a7b6844c8fdb34a7aa3d13ef962c4e9568043648214f3703b82c2`

假 sealed path 的 opener/resolver 未被调用。第二次写同一文件 `FileExistsError`。未 finalize 的 run 不能标 complete。
